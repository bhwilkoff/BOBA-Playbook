# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.moved-aside
*.pbxuser
*.perspectivev3
!default.pbxuser
!default.perspectivev3
*.mode1v3
*.mode2v3
!default.mode1v3
!default.mode2v3
xcshareddata/WorkspaceSettings.xcsettings

# Swift Package Manager
.build/
Packages/
Package.pins
Package.resolved

# iOS
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods (if ever used)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# Node (shouldn't be needed, but just in case)
node_modules/

# Next.js build artifacts (tools/screenshots dev server)
.next/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
*.key
*.p8
credentials.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Private business / monetization strategy
MONETIZATION.md
RADISH_PARTNERSHIP_CALL.md
pipeline/eval/

# Python bytecode (pipeline scripts) — was leaking
# scripts/__pycache__/*.pyc into commits before tick 220
__pycache__/
*.pyc
*.pyo
.tmp-logcat.txt

# Local pricing-crawl resume cursors (generated by scripts/crawl_active_listings.py)
scripts/.active_crawl_cursor.json
scripts/.active_crawl_cursor_whatnot.json

# Image-sourcing tooling + data — kept locally to pull future card sets, but
# never committed (we don't surface how/where the catalog art is sourced).
# The shipped catalog (cards.json + bundles) carries imageFile only, no source.
pipeline/scripts/bv_catalog_scraper.py
pipeline/scripts/bv_api_harvest.py
pipeline/scripts/bv_tecmo_backfill.py
pipeline/scripts/scrape_promo_tecmo.py
pipeline/scripts/tecmo_release_watch.py
pipeline/scripts/stage_a_scrape_src.py
scripts/import_tecmo_bowl.py
pipeline/data/tecmo/
pipeline/data/scan_results.csv
