# Good morning! Overnight loop summary

> Updated by the autonomous /loop. Skim top-down. Things you need to do are at the bottom.

## Loop config
- Cadence: every 60s via CronCreate `* * * * *` (session-only — dies if Claude exits)
- Sentinel: `<<autonomous-loop>>`
- Platform cadence: tick%5 → 0=opt, 1+4=Android, 2=iOS, 3=web

## Standing priorities (from your message)
1. **Parity across all 3 platforms** — Web should be ~full-featured (modulo 3D); iOS flagship; Android first-class mobile
2. **New features** — sourced from the official BoBA blog archive (`docs/blog-digest.md`, refreshed daily) and proactive web research on TCG-collecting apps + BoBA content. Every 5th tick must include research, not just first-tick research. (Discord exports dir was referenced earlier but doesn't exist in repo — blog archive is the live source.)
3. **Code optimization** — every tick%5==0 lowers complexity / removes cruft / cuts file size. PR diff should net-remove lines.

## What shipped overnight

<!-- Each tick appends a one-line summary here. Most recent on top. -->
- **tick 508 (web — audit only, polish surface saturated)** — Full audit of web checkbox / toggle / input UX vs Android tick 504/506 row-toggle pattern: all web checkboxes are properly `<label>`-wrapped (custom-rainbow-editor-inspiredink, wall-price-toggle, has-image-checkbox, store-include-bigbox, profile-public-toggle, practice-super-ties/sudden-death) so clicking anywhere on the label flips them — web doesn't have the micro-target problem Android M3 ListItem has. Other audited surfaces: Open Graph meta confirmed twitter-free (DECISIONS.md #053 + the explicit "NO twitter:* tags" comment at app.js L611), Tournament Events freshness stamp uses relativeDate, Card Detail modal action row fully tooltipped (484/488/498), public-collection Share + Wall buttons wired. Polish surface saturated; remaining web work is multi-tick features (Wanted-list sharing schema, Discord-link state row in Profile). No code change this tick.
- **tick 507 (iOS v2.359 / 621 — drop iOS-18 @available guards on Hero Shot button)** — Audit during tick: iOS Switch sites all use SwiftUI Toggle which is row-tappable by default (no port for Android tick 504/506 row-toggle pattern owed). Concrete polish: dropped two `if #available(iOS 18.0, *)` guards on the CollectionCardDetailView Hero Shot button + its fullScreenCover. Per `feedback_ios_26_baseline` the rule is "remove when touching the file, don't sweep proactively" — I've been touching CollectionCardDetailView this session (tick 444 / 447 / 502 / 505), so this counts as touched. Both guards are no-ops on iOS 26+ baseline. Net −2 lines. Other iOS-18 guards in BOBAPlaybook/ (HouseOfCardsView + BOBAPlaybookApp HeroShotCLIRunner) remain until those files get touched for unrelated work.
- **tick 506 (Android — row-level toggle on ProfileSheet ToggleRow + FilterSheet HasImageRow)** — Continuing the tick-504 pattern. Audited all 3 Switch sites in feature/: CustomRainbowEditorSheet (closed tick 504), ProfileSheet.ToggleRow (used by push/match-alerts/public-collection/trading toggles), and FilterSheet.HasImageRow. Both remaining sites had the same micro-target-only Switch hit zone — users had to thumb the 32dp widget. Wrapped both in `Modifier.toggleable(value, role: Switch, onValueChange)` + flipped `Switch.onCheckedChange = null` (toggleable owns state). ProfileSheet ToggleRow is the bigger win since it's the prefs-list pattern (4+ rows in Profile section).
- **tick 505 (opt + research — extract `.cardThumbBadges(for:)` View extension)** — Research: CI green across recent commits. Opt: 3 sites had identical 8-line inline overlay blocks combining PrintRunBadge (corner) + FormatLegalityHintBadge (bottom-leading): Search/CardDetailView Other Versions, Collection/CollectionCardDetailView variationTile (tick 502), and Collection/RainbowDetailView grid (tick 477). Extracted `extension View { func cardThumbBadges(for card, printRunAlignment, padding) }` in Design.swift. Each call site shrinks from 8 lines to 1; RainbowDetailView still needs `printRunAlignment: .topLeading` override because its top-trailing carries an owned-checkmark. Net ≈ −1 line (the helper boilerplate offsets most call-site savings) but single source of truth for all card-thumb-badge surfaces.
- **tick 504 (Android — CustomRainbow editor 'Inspired Ink only' row tap-toggles)** — Found a real UX gap on Android CustomRainbowEditorSheet: the "Inspired Ink only" Switch row had `Switch + Text` side-by-side but no row-level click handler. Users had to thumb-target the 32dp Switch widget exactly even though the long label takes most of the row's width. Material 3 convention is row-level toggle; without it, the affordance is unnecessarily small. Wrapped the Row in `Modifier.toggleable(value:, role: Switch, onValueChange:)` + flipped `Switch.onCheckedChange = null` (toggleable handles the change). `fillMaxWidth` ensures the whole row is tappable. Same pattern Material 3 uses on `ListItem` rows with trailing switches.
- **tick 503 (web — Profile avatar button title= tooltip)** — Audit during tick: web Other Versions tiles already ship both badges (tick 463 print-run + tick 473 format-legality) + tap-to-navigate via openModal (line 2938). No iOS tick 502 port owed — web's Card Detail modal handles all Other-Versions surfaces through one path. Smaller polish found: `#profile-avatar-btn` had aria-label="Change profile picture" but no `title=` attr. Added matching title for hover-hint on desktop pointer users. Avatar menu rows (Choose / Discord / Remove / Cancel) are text-labeled action items so no title= needed. Other Profile button audit clean.
- **tick 502 (iOS v2.358 / 620 — variation tiles get print-run + format-legality badges)** — Audit caught iOS CollectionCardDetailView's `variationTile` (private function rendering Other Versions tiles) was missing both badges that the catalog-side CardDetailView's tiles got at tick 462/472. Android closed this on its CollectionCardDetailScreen at tick 461/469 via BOBACardCell wiring. iOS Collection-side bypasses BOBACardCell (uses CardImageView directly), so the badges need inline overlays. Added `.overlay(alignment: .topTrailing)` PrintRunBadge + `.overlay(alignment: .bottomLeading)` FormatLegalityHintBadge via the extracted structs (tick 465/475/497). Closes the badge parity across iOS's two Other-Versions surfaces (Find-tab catalog detail + Collection-side detail) — both now show both badges.
- **tick 501 (Android — CollectionCardDetail Other Versions navigation, closes tick 499 bug)** — Fixed the real bug tick 499 flagged: CollectionCardDetailScreen's "Other versions" tiles had a comment promising clickability but no actual `Modifier.clickable`. Added `onOtherVersionClick: (String) -> Unit` parameter to the screen + wired `Modifier.clickable { onOtherVersionClick(other.bobaId) }` on each tile's Box. BOBAApp.kt nav host now passes `{ navController.navigate(NavRoutes.collectionCardDetail(it)) }`. Tapping a variant tile pushes that variant's detail onto the back stack — iOS CardDetailView's NavigationLink behavior at line 920 matched. Updated the inline comment to reflect actual behavior. Closes the 2-platform Other-Versions navigation parity (iOS shipped this from the start).
- **tick 500 (opt + research, net −13 lines — TICK-500 MILESTONE)** — 500-tick milestone. Research: CI green across all 3 platforms. State of the loop: 200+ ticks past the post-compaction restart (tick 202); platform-cadence rotation has shipped print-run + format-legality badges everywhere, closed Sign-in-with-Google on web + iOS, added Add-to-Deck affordance + ShareLink for rainbow on all 3 platforms, exhaustive BOBAIconTooltip rollout on Android (26 sites), saturated `.help()` rollout on iOS, exhaustive `title=` coverage on web. Discord-mined backlog #1-8 closed except #6 (Wanted-list public sharing, multi-week schema). P1 backlog "Web sign-in with Google" closed tick 483. Remaining open work: orphan DeckBuilderView (P0 multi-step iOS extraction), Value history chart + Saved Searches (🔮 all 3, multi-tick), Android M5.5 Practice + M7 BiometricPrompt (multi-tick), Other Versions Android navigation (tick 499 flagged). Opt: trimmed 14 archived WAKE_UP.md tick entries (476-489) following the rolling-window pattern (435/450/470/490). Net −13 lines.
- **tick 499 (Android — doc fix on CollectionCardDetail Other Versions, behavior gap flagged)** — Audit caught a real bug: the comment above CollectionCardDetailScreen's Other Versions LazyRow promised "Tap an alt to navigate to that card's detail" but the `Box` had no `Modifier.clickable` and the BOBACardCell wasn't passed an onClick — tiles are NOT tappable today. iOS CardDetailView's Other Versions tiles DO push via NavigationLink (CardDetailView.swift L920). Fixing the navigation properly requires threading an `onOtherVersionClick` callback through `BOBAApp.kt`'s nav host — multi-step, queued for a future tick. For this tick, fixed the misleading comment to reflect actual behavior (read-only previews). Note documents the parity gap + iOS reference for the future implementation.
- **tick 498 (web — Add-to-deck toast when all cards are already in deck)** — Edge-case audit caught `bulkAddToDeck` emits "Added 0 cards to {deck} · 1 duplicate skipped" when the user taps "Add to Deck" on a card that's already in the chosen deck — awkward wording. Tick 478's Card Detail "Add to Deck" button makes this single-card flow common. Branched the toast: single-card already-in shows "Already in {deck}"; multi-card all-dup shows "All N cards already in {deck}". Normal happy-path toast unchanged. Closes a minor UX wart in the new add-to-deck flow.
- **tick 497 (iOS v2.357 / 619 — extract CardCornerBadge, Android tick 495 parity)** — Audit while looking for iOS polish to mirror Android tick 496's hero-projection fix: iOS AddToDeckSheet has different UX (pick-a-deck list, not draft-context projection) so no direct port owed. Instead: closed the cross-platform-consistency parity with Android tick 495 — iOS BOBACardCell had PrintRunBadge + FormatLegalityHintBadge as sibling structs with identical Text + font + padding + bg + clipShape, differing only in `foregroundStyle`. Extracted shared `struct CardCornerBadge: View { let label: String; let accent: Color }`. Both wrappers become 1-3 line passes. Net +4 lines (Swift struct boilerplate slightly heavier than Kotlin's; Android tick 495 had -11), but the bigger value is single source of truth matching Android.
- **tick 496 (Android — AddToDeckSheet projects hero count too)** — Found a real consistency gap on Android: the AddToDeckSheet "Current draft" stats line projected plays + HD + DBS post-add, but `heroCount` showed CURRENT (pre-add) — so a coach about to add their 5th hero into an 8-cap deck saw "4 heroes" instead of "5/8 heroes." Added `projectedHeroes = draft.heroCount + (if (card.isHero) 1 else 0)` + corresponding `overHeroes` flag; updated the stats Text to show `$projectedHeroes/${draft.heroCap} heroes` (matching the plays/HD format) + included `overHeroes` in the red-warning branch. All 4 stats now consistently project post-add state. iOS DeckBuilderView already does this per the comment.
- **tick 495 (opt + research — extract Android CardCornerBadge, net −11 lines)** — Research: CI green for the 5-commit run since tick 489's parity fix. Recent tick shape: tick 491-494 covered FQN Color sweep + tick 492 added iOS Google sign-in + tick 493 closed rainbow guard parity. Opt: Android BOBACardCell had PrintRunBadge + FormatLegalityHintBadge as two near-identical Composables (Surface + Text with alpha-0.62 black bg + corner radius 4 + labelSmall.Bold). Only `color = accent` differed. Same DRY shape as iOS PrintRunBadge/FormatLegalityHintBadge structs (tick 465/475 — kept as siblings there because each adds ~3 lines). On Android the Composable boilerplate is heavier (Surface wrapper + Text params) so extraction pays off. New `CardCornerBadge(label, accent, modifier)` private composable holds the shared layout; the two wrappers shrink to 1-3 line passes. Net −11 lines + single source of truth for chip visual treatment.
- **tick 494 (Android — FQN Color → short import sweep, 5 files / 11 sites)** — Finishes the FQN Color cleanup tick 491 deferred. 5 feature files needed `import androidx.compose.ui.graphics.Color` + inline FQN → short replacement: CardDetailScreen (6 sites), PurchaseScreen (2), RainbowDetailScreen / CollectionScreen / DeckWallSheet (1 each). Net +5 imports / -11 FQN-chars (line count +5 — imports add lines, inline FQN→short same-line). Same FQN-cleanup pattern as ticks 471 + 486 + 491. All `androidx.compose.ui.graphics.Color(` FQN call sites in feature/ now cleaned. (Note: this isn't an opt tick — net-positive line count — but it's appropriate Android code-style cleanup work since FQN cleanup tracks ongoing per `feedback_kotlin_extension_calls_unqualified` adjacent style consistency.)
- **tick 493 (web — Rainbow share button defensive guard, iOS tick 482 + Android tick 489 parity)** — Closes the 3-platform defensive-guard parity for rainbow share. iOS tick 482 + Android tick 489 both hide/disable the Share affordance when the rainbow has zero matching cards — otherwise the share text would be "My X rainbow: 0 of 0 cards (0%) · bobaplaybook.com" nonsense. Web's `_renderRainbowRow` was unconditionally rendering `.rainbow-share-btn` regardless of `matching.length`. Now conditionally emits empty string when matching is empty — matches iOS hide-entirely behavior (Android shows disabled state instead).
- **tick 492 (iOS v2.356 / 618 — Sign in with Google button, web tick 483 parity)** — Web tick 483 added Google sign-in; Android Credential Manager has Google as primary auth. iOS had only Apple + Discord OAuth + email. Per DECISIONS.md #050, Apple stays primary on iOS — added Google as a third option between Apple and Discord. New `AuthManager.signInWithGoogle()` reuses the existing `startOAuthFlow(provider: "google")` machinery (PKCE + ASWebAuthenticationSession) — same flow as Discord, no captureDiscordIdentityFromOAuth() step. New `googleButton` view in SignInView (white surface + black border + Google blue G glyph), plus a placeholder `GoogleGlyphView` (Google blue circle with white "G" — not the full 4-color brand glyph; queued for a future Assets.xcassets/google-logo.imageset asset). Requires Google provider enabled in Supabase Auth dashboard — same Ben-action that tick 483 added to WAKE_UP "Queued from recent loop ticks" already covers iOS.
- **tick 491 (Android — FQN Color → short in ProfileSheet, 4 sites)** — Audit during tick: Android polish surface mature (29 of 49 IconButtons tooltipped; remaining 20 are universal close/back/search-clear idioms per the tick 446/451 policy). One concrete cleanup found: 4 inline FQN `androidx.compose.ui.graphics.Color(0xFF...)` call sites in ProfileSheet — `Color` already imported at line 68 so the FQN was redundant. Cleaned each: 2 Discord-blue tints + 2 ProviderPill tints (Google #4285F4 + Discord #5865F2). Same FQN-cleanup pattern as ticks 471 + 486. Noted but skipped: 10 more FQN Color sites across RainbowDetail / CollectionScreen / DeckWallSheet / PurchaseScreen / CardDetailScreen where `Color` isn't yet imported — adding 5 imports + cleaning across 5 files is a multi-file sweep that risks more failure modes; queued for a future opt-tick if needed.
- **tick 490 (opt + research, net −29 lines — milestone)** — Milestone tick. Research: CI green since tick 476's import fix; the in-progress runs at tick 490 launch are the post-489 batch. Recent feature shape: P1 backlog item "Web sign-in with Google" closed tick 483; Add-to-Deck affordance now ships on iOS + Android + web Card Detail; ShareLink for rainbow progress on all 3 platforms; format-legality + print-run badges fully rolled out. Remaining open work: #6 Wanted-list public sharing (multi-week schema), orphan DeckBuilderView (P0 multi-step), Value history chart + Saved Searches (🔮 all 3), Android M5.5 Practice + M7 BiometricPrompt (multi-tick). Opt: trimmed 30 archived WAKE_UP.md tick entries (446-475) — same rolling-window approach as tick 470 (cut 416-445) + tick 450 (cut 380-415) + tick 435 (cut 329-379). All polish patterns preserved in git history + memory files. Recent ticks (476-489) kept visible for context.

## Things you (Ben) need to do

<!-- Anything that needs human action lands here. -->

### Still queued from before the loop started
1. **Manual-upload `app-release.aab`** (32 MB at `android/app/build/outputs/bundle/release/app-release.aab`) → Play Console → Internal Testing → Create new release.
2. **Walk through Play Console required forms** using `android/distribution/play-console-listing.md`.
3. **Upload icon + feature graphic + 5 screenshots** from `tools/play-assets/out/`.
4. **Re-capture screenshots 2–5** by booting the Pixel 9 Pro AVD from Android Studio's Device Manager + running `bash tools/play-assets/capture-screens.sh`.
5. **Add internal-testing emails**, share opt-in URL.
6. **Promote to Open Testing** track when ready.

### Queued from recent loop ticks
7. **Enable Google as an OAuth provider in Supabase Auth dashboard** (tick 483). Web's new "Sign in with Google" button calls `supa().auth.signInWithOAuth({ provider: 'google' })`; the button is shipped but the provider must be enabled server-side for the OAuth flow to succeed. Steps: Supabase Auth → Providers → Google → toggle + paste OAuth Client ID + Secret from Google Cloud Console.

## How to stop the loop

```sh
# In Claude Code:
/cron list
/cron delete <id>
```

Or just exit Claude — session-only cron dies automatically.
