# Good morning! Overnight loop summary

> Updated by the autonomous /loop. Skim top-down. Things you need to do are at the bottom.

## Loop config
- Cadence: every 60s via CronCreate `* * * * *` (session-only — dies if Claude exits)
- Sentinel: `<<autonomous-loop>>`
- Platform cadence: tick%5 → 0=opt, 1+4=Android, 2=iOS, 3=web

## Standing priorities (from your message)
1. **Parity across all 3 platforms** — Web should be ~full-featured (modulo 3D); iOS flagship; Android first-class mobile
2. **New features** — sourced from the official BoBA blog archive (`docs/blog-digest.md`, refreshed daily) and proactive web research on TCG-collecting apps + BoBA content. Every 5th tick must include research, not just first-tick research. (Discord exports dir was referenced earlier but doesn't exist in repo — blog archive is the live source.)
3. **Code optimization** — every tick%5==0 lowers complexity / removes cruft / cuts file size. PR diff should net-remove lines.

## What shipped overnight

<!-- Each tick appends a one-line summary here. Most recent on top. -->
- **tick 498 (web — Add-to-deck toast when all cards are already in deck)** — Edge-case audit caught `bulkAddToDeck` emits "Added 0 cards to {deck} · 1 duplicate skipped" when the user taps "Add to Deck" on a card that's already in the chosen deck — awkward wording. Tick 478's Card Detail "Add to Deck" button makes this single-card flow common. Branched the toast: single-card already-in shows "Already in {deck}"; multi-card all-dup shows "All N cards already in {deck}". Normal happy-path toast unchanged. Closes a minor UX wart in the new add-to-deck flow.
- **tick 497 (iOS v2.357 / 619 — extract CardCornerBadge, Android tick 495 parity)** — Audit while looking for iOS polish to mirror Android tick 496's hero-projection fix: iOS AddToDeckSheet has different UX (pick-a-deck list, not draft-context projection) so no direct port owed. Instead: closed the cross-platform-consistency parity with Android tick 495 — iOS BOBACardCell had PrintRunBadge + FormatLegalityHintBadge as sibling structs with identical Text + font + padding + bg + clipShape, differing only in `foregroundStyle`. Extracted shared `struct CardCornerBadge: View { let label: String; let accent: Color }`. Both wrappers become 1-3 line passes. Net +4 lines (Swift struct boilerplate slightly heavier than Kotlin's; Android tick 495 had -11), but the bigger value is single source of truth matching Android.
- **tick 496 (Android — AddToDeckSheet projects hero count too)** — Found a real consistency gap on Android: the AddToDeckSheet "Current draft" stats line projected plays + HD + DBS post-add, but `heroCount` showed CURRENT (pre-add) — so a coach about to add their 5th hero into an 8-cap deck saw "4 heroes" instead of "5/8 heroes." Added `projectedHeroes = draft.heroCount + (if (card.isHero) 1 else 0)` + corresponding `overHeroes` flag; updated the stats Text to show `$projectedHeroes/${draft.heroCap} heroes` (matching the plays/HD format) + included `overHeroes` in the red-warning branch. All 4 stats now consistently project post-add state. iOS DeckBuilderView already does this per the comment.
- **tick 495 (opt + research — extract Android CardCornerBadge, net −11 lines)** — Research: CI green for the 5-commit run since tick 489's parity fix. Recent tick shape: tick 491-494 covered FQN Color sweep + tick 492 added iOS Google sign-in + tick 493 closed rainbow guard parity. Opt: Android BOBACardCell had PrintRunBadge + FormatLegalityHintBadge as two near-identical Composables (Surface + Text with alpha-0.62 black bg + corner radius 4 + labelSmall.Bold). Only `color = accent` differed. Same DRY shape as iOS PrintRunBadge/FormatLegalityHintBadge structs (tick 465/475 — kept as siblings there because each adds ~3 lines). On Android the Composable boilerplate is heavier (Surface wrapper + Text params) so extraction pays off. New `CardCornerBadge(label, accent, modifier)` private composable holds the shared layout; the two wrappers shrink to 1-3 line passes. Net −11 lines + single source of truth for chip visual treatment.
- **tick 494 (Android — FQN Color → short import sweep, 5 files / 11 sites)** — Finishes the FQN Color cleanup tick 491 deferred. 5 feature files needed `import androidx.compose.ui.graphics.Color` + inline FQN → short replacement: CardDetailScreen (6 sites), PurchaseScreen (2), RainbowDetailScreen / CollectionScreen / DeckWallSheet (1 each). Net +5 imports / -11 FQN-chars (line count +5 — imports add lines, inline FQN→short same-line). Same FQN-cleanup pattern as ticks 471 + 486 + 491. All `androidx.compose.ui.graphics.Color(` FQN call sites in feature/ now cleaned. (Note: this isn't an opt tick — net-positive line count — but it's appropriate Android code-style cleanup work since FQN cleanup tracks ongoing per `feedback_kotlin_extension_calls_unqualified` adjacent style consistency.)
- **tick 493 (web — Rainbow share button defensive guard, iOS tick 482 + Android tick 489 parity)** — Closes the 3-platform defensive-guard parity for rainbow share. iOS tick 482 + Android tick 489 both hide/disable the Share affordance when the rainbow has zero matching cards — otherwise the share text would be "My X rainbow: 0 of 0 cards (0%) · bobaplaybook.com" nonsense. Web's `_renderRainbowRow` was unconditionally rendering `.rainbow-share-btn` regardless of `matching.length`. Now conditionally emits empty string when matching is empty — matches iOS hide-entirely behavior (Android shows disabled state instead).
- **tick 492 (iOS v2.356 / 618 — Sign in with Google button, web tick 483 parity)** — Web tick 483 added Google sign-in; Android Credential Manager has Google as primary auth. iOS had only Apple + Discord OAuth + email. Per DECISIONS.md #050, Apple stays primary on iOS — added Google as a third option between Apple and Discord. New `AuthManager.signInWithGoogle()` reuses the existing `startOAuthFlow(provider: "google")` machinery (PKCE + ASWebAuthenticationSession) — same flow as Discord, no captureDiscordIdentityFromOAuth() step. New `googleButton` view in SignInView (white surface + black border + Google blue G glyph), plus a placeholder `GoogleGlyphView` (Google blue circle with white "G" — not the full 4-color brand glyph; queued for a future Assets.xcassets/google-logo.imageset asset). Requires Google provider enabled in Supabase Auth dashboard — same Ben-action that tick 483 added to WAKE_UP "Queued from recent loop ticks" already covers iOS.
- **tick 491 (Android — FQN Color → short in ProfileSheet, 4 sites)** — Audit during tick: Android polish surface mature (29 of 49 IconButtons tooltipped; remaining 20 are universal close/back/search-clear idioms per the tick 446/451 policy). One concrete cleanup found: 4 inline FQN `androidx.compose.ui.graphics.Color(0xFF...)` call sites in ProfileSheet — `Color` already imported at line 68 so the FQN was redundant. Cleaned each: 2 Discord-blue tints + 2 ProviderPill tints (Google #4285F4 + Discord #5865F2). Same FQN-cleanup pattern as ticks 471 + 486. Noted but skipped: 10 more FQN Color sites across RainbowDetail / CollectionScreen / DeckWallSheet / PurchaseScreen / CardDetailScreen where `Color` isn't yet imported — adding 5 imports + cleaning across 5 files is a multi-file sweep that risks more failure modes; queued for a future opt-tick if needed.
- **tick 490 (opt + research, net −29 lines — milestone)** — Milestone tick. Research: CI green since tick 476's import fix; the in-progress runs at tick 490 launch are the post-489 batch. Recent feature shape: P1 backlog item "Web sign-in with Google" closed tick 483; Add-to-Deck affordance now ships on iOS + Android + web Card Detail; ShareLink for rainbow progress on all 3 platforms; format-legality + print-run badges fully rolled out. Remaining open work: #6 Wanted-list public sharing (multi-week schema), orphan DeckBuilderView (P0 multi-step), Value history chart + Saved Searches (🔮 all 3), Android M5.5 Practice + M7 BiometricPrompt (multi-tick). Opt: trimmed 30 archived WAKE_UP.md tick entries (446-475) — same rolling-window approach as tick 470 (cut 416-445) + tick 450 (cut 380-415) + tick 435 (cut 329-379). All polish patterns preserved in git history + memory files. Recent ticks (476-489) kept visible for context.
- **tick 489 (Android — RainbowDetailScreen Share button enabled-guard, iOS tick 482 parity)** — Audit: iOS RainbowDetailView's `rainbowShareText` computed property returns nil when context isn't resolved, so the ShareLink ToolbarItem skips entirely (tick 482 implementation). Android's IconButton onClick callback had defensive `if (allCards.isEmpty()) 0 else ...` math but rendered the button unconditionally — tapping before data loads would share a nonsense "0 of 0 cards (0%)" payload. Added `enabled = allCards.isNotEmpty()` to the IconButton, simplified the pct computation now that the empty case is no longer reachable, and updated the surrounding comment. Closes the 2-platform Share-defensive-guard parity. (Other Android polish surfaces audited this tick — ProfileSheet TextButtons text-labeled, Decks overflow menu items already iconified at tick 474 + ongoing, WatchPage SegmentedButton-icon suppression intentional. Rollout mature.)
- **tick 488 (web — title= tooltips on Card Detail 'Add to Collection' buttons)** — Audit caught both Card Detail modal "Add to Collection" variants (regular + sealed-product, lines 3769 + 3840) had visible text labels but no `title=` attr. Adjacent buttons "Add to Deck" (tick 478) and "Share" (tick 438) both already carry title= — these were the only two outliers. Added `title="Add this card to your collection"` (regular) + `title="Add this sealed product to your collection"` (sealed variant). Closes the per-button hover-hint pattern for Card Detail modal action row.
- **tick 487 (iOS v2.355 / 617 — ShowDetailView 'Mark as big hit' .help() tooltip)** — Audit of iOS .accessibilityLabel sites that lack matching .help(): ShowDetailView's per-row "Mark as big hit" star button (line 308) had accessibilityLabel but no .help() — streamers use this regularly when curating cards for live shows + the visual star alone doesn't communicate the bigHit semantic. Added `.help(bigHit ? "Remove big-hit emphasis" : "Mark as big hit")` matching the existing dynamic accessibilityLabel. Other audited candidates either already had .help() (Scan/Filters/More on CollectionView per tick 392; Hero Shot per tick 442; Edit-copy per tick 447; Edit-rainbow/Share-rainbow per tick 447/482; Show-wall-share per tick 442) OR were text-labeled buttons where a tooltip would be redundant (UNDO deck load, CollectionShowcase Pause/Resume).
- **tick 486 (Android — FQN Icons.* → imported short form, 5 call sites)** — Audit: 5 leftover FQN `androidx.compose.material.icons.Icons.Default.*` call sites in feature/ (RainbowDetail Share + CollectionScreen Check + DeckSecondaryScreens Search/Clear + CardDetail Bolt). All 4 icons already imported in their respective files via `androidx.compose.material.icons.filled.X` — the FQN was redundant. Cleaned each to the imported short form (`Icons.Default.X`). Code-style consistency with the rest of the file's icon usage. Net 0 lines (FQN-to-short on same line), reads cleaner.
- **tick 485 (opt + research, net −14 lines)** — Research: CI green for the iOS + Android + web ticks since 480's milestone. Backlog: 9 sealed products + orphan DeckBuilderView (P0) + multi-week features (P1) remain. Opt: tick 478's "Add to Deck" web button duplicated ~17 lines of the existing `openDeckPicker` (auth gate + decks fetch + empty toast + popover render) — the only differences were how cards are sourced (`[card]` vs `getSelectedCardObjects()`) and the popover title. Extracted to `promptDeckPickerForCards(cards, anchor, titleOverride)`. The old `openDeckPicker(e)` becomes a 1-line wrapper passing the multi-select source; the tick-478 inline handler shrinks from 17 lines to 3. Single source of truth for the deck-picker UX flow + cleaner extensibility for future card-add affordances. Net −14 lines.
- **tick 484 (Android — Practice admin-unlock tooltip + Ben-action: enable Google in Supabase)** — Two small additions. (1) The last un-wrapped IconButton in ProfileSheet was the admin-only Practice unlock (Bolt icon at L1257). Wrapped in `BOBAIconTooltip("Practice executor (admin only)")` — completes the ProfileSheet tooltip coverage. (2) Added a "Queued from recent loop ticks" subsection to WAKE_UP "Things you (Ben) need to do" with one item: enable Google as an OAuth provider in Supabase Auth dashboard (the prereq for tick 483's "Sign in with Google" button to actually sign users in). 26th BOBAIconTooltip call site app-wide.
- **tick 483 (web — Sign in with Google button on auth modal, P1 backlog)** — Backlog item P1 "Web sign-in with Google" delivered. Android Credential Manager already ships Google as the primary mobile auth; iOS uses Apple-first so doesn't carry Google. Web previously had Apple + Discord OAuth + email; Google was the gap. Added `authSignInWithGoogle()` to api.js (mirrors authSignInWithApple/Discord — `supa().auth.signInWithOAuth({ provider: 'google', options: { redirectTo } })`). New `#auth-google-btn` button in the modal markup using the standard Google multi-color "G" SVG. New `.btn-google-signin` CSS rule mirroring Apple's white-surface style + Discord's vertical spacing. Click handler routes through API.authSignInWithGoogle(). Provider must be enabled in Supabase Auth dashboard for the button to work — otherwise the OAuth call fails gracefully. Marks P1 #4 done on the AUTONOMOUS_PROGRESS backlog (per tick 480's research note).
- **tick 482 (iOS v2.354 / 616 — Share rainbow progress ShareLink, closes 3-platform trio)** — Audit caught a real iOS parity gap: tick 439's WAKE_UP note that "iOS RainbowDetailView already uses native ShareLink" was inaccurate — iOS had no share affordance at all. Android tick 439 + web tick 443 both ship "Share rainbow progress." Added a `ShareLink` ToolbarItem.topBarTrailing next to the existing Edit slider (slider.horizontal.3). Uses the same text shape Android emits: "My X rainbow: 5 of 12 treatments (41%) · bobaplaybook.com". Auto-rainbows → "treatments" unit; custom rainbows → "cards" unit. New `rainbowShareText` computed property recomputes lazily so the ShareLink stays accurate as the user collects more. Both toolbar buttons (Edit + Share) co-exist for custom rainbows; auto-rainbows only see Share (Edit gated on isCustom). Closes the 3-platform rainbow-share parity trio.
- **tick 481 (Android — gate 'Add to Deck' menu item on deckable card types)** — Audit during tick: iOS CardDetailView gates the "To Custom Deck" Label with `if card.isHero || card.isPlay || card.isHotDog` (CardDetailView.swift L242); web tick 478 carried the same gate. Android's DropdownMenuItem was unconditional — Sealed Products showed "Add to Deck" which leads to a confused "you can't add this" state. Wrapped the DropdownMenuItem in `if isDeckable` using the existing `state.card.isHero || isPlay || isHotDog` properties on the Card model. Closes the 3-platform deck-add gate parity.
- **tick 480 (opt + research, net −25 lines — milestone)** — Milestone tick. Research: CI green for tick 478 (web) + 479 (Android) commits since tick 476's CI-fix. WAKE_UP at 73 lines / 34 ticks — within rolling-window target. Backlog reaffirms multi-week items (Wanted-list sharing, Practice executor port, BiometricPrompt, sealed products) as the only big remaining work; ambient polish well-saturated. Opt: trimmed the "Archived M0 details (kept for reference)" 24-line scaffold checklist from SCRATCHPAD.md. M0 shipped 280+ ticks ago (2026-05-19); the bulleted scaffold list ("✅ /android/ Gradle project on the AGP 9.2.0 stack..." etc.) was pure CLAUDE.md "rot as the codebase evolves" — every line described state that's been the working baseline for hundreds of ticks. Git history captures the build-up; the load-bearing "Resolved direction" + "Shipped screens" + "Deferred follow-ups" blocks above stay verbatim.
- **tick 479 (Android — BOBAIconTooltip on Refresh icons in CardDetail + Purchase)** — Continuing the BOBAIconTooltip rollout. Two Refresh IconButtons still carried `contentDescription` but no tooltip: (a) CardDetailScreen pricing-section Refresh (line 1299) — wrapped in `BOBAIconTooltip("Refresh pricing")`, (b) PurchaseScreen breaks Refresh (line 107) — wrapped in `BOBAIconTooltip("Refresh upcoming breaks")`. Both surface hover/long-press hints before firing. Verified Android CI green for tick 476's import fix (run 3771a26 success). 25th BOBAIconTooltip call site app-wide.
- **tick 478 (web — Add to Deck button on Card Detail modal, iOS + Android parity)** — Audit during tick: web Card Detail modal had only "Add to Collection" + "Share" buttons; "Add to Deck" was missing despite iOS shipping it via Label("To Custom Deck", systemImage: "rectangle.stack.badge.plus") and Android shipping it via DropdownMenuItem at tick 474. Added a new `<button data-action="add-to-deck">` button conditional on `card.cardType === Hero|Play|HotDog` (skipping Sealed Products — same gate as iOS). Lucide layers-2 SVG icon (stack of cards). Click handler reuses the existing `openDeckPicker` / `bulkAddToDeck` plumbing from multi-select by injecting `[card]` as a single-card array. Closes the 3-platform Card Detail "Add to Deck" parity. (Web rainbow `_renderRainbowThumbs` parity from tick 477 was reconsidered — at 56px thumb width the badges would be too small to read; deferred indefinitely.)
- **tick 477 (iOS v2.353 / 615 — print-run + format-legality badges on Rainbow tiles, Android tick 459 parity)** — iOS RainbowDetailView's `cell(card:isOwned:)` bypasses BOBACardCell — uses `CardImageView(card:, size:.thumb)` directly — so neither badge surfaced there despite shipping on Find / Decks pool / Collection at tick 457/467. Android RainbowDetailScreen uses BOBACardCell so it gets both. Added the two inline overlays using the extracted `PrintRunBadge` + `FormatLegalityHintBadge` structs (tick 465 + 475 extractions made this clean). Placement: print-run top-LEADING (top-trailing is taken by the owned `checkmark.seal.fill`), format-legality bottom-LEADING. Closes the 2-platform RainbowDetail both-badges parity. Web rainbow rows render a thumbnail strip via separate `_renderRainbowThumbs` — could add similar inline overlays in a future web tick.
- **tick 476 (Android, CI fix — LibraryAdd / PlaylistAdd imports wrong path)** — Android · build went red on tick 474's icon additions. The CI error was `Unresolved reference 'LibraryAdd'` at the import line. I'd assumed LibraryAdd + PlaylistAdd were in `androidx.compose.material.icons.automirrored.filled.*` (like ArrowBack), but they live in plain `androidx.compose.material.icons.filled.*` — they're not RTL-mirrored. Fixed imports + the two `Icons.AutoMirrored.Filled.X` call sites to `Icons.Default.X`. Loop guidance "CI went red trumps cadence" — this tick is a defensive fix, not Android polish. Adds to the [[feedback_autonomous_loop_failure_modes]] memory: not all "add" icons are AutoMirrored; check the actual source-of-truth import path before assuming.

## Things you (Ben) need to do

<!-- Anything that needs human action lands here. -->

### Still queued from before the loop started
1. **Manual-upload `app-release.aab`** (32 MB at `android/app/build/outputs/bundle/release/app-release.aab`) → Play Console → Internal Testing → Create new release.
2. **Walk through Play Console required forms** using `android/distribution/play-console-listing.md`.
3. **Upload icon + feature graphic + 5 screenshots** from `tools/play-assets/out/`.
4. **Re-capture screenshots 2–5** by booting the Pixel 9 Pro AVD from Android Studio's Device Manager + running `bash tools/play-assets/capture-screens.sh`.
5. **Add internal-testing emails**, share opt-in URL.
6. **Promote to Open Testing** track when ready.

### Queued from recent loop ticks
7. **Enable Google as an OAuth provider in Supabase Auth dashboard** (tick 483). Web's new "Sign in with Google" button calls `supa().auth.signInWithOAuth({ provider: 'google' })`; the button is shipped but the provider must be enabled server-side for the OAuth flow to succeed. Steps: Supabase Auth → Providers → Google → toggle + paste OAuth Client ID + Secret from Google Cloud Console.

## How to stop the loop

```sh
# In Claude Code:
/cron list
/cron delete <id>
```

Or just exit Claude — session-only cron dies automatically.
