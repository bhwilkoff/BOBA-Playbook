# Good morning! Overnight loop summary

> Updated by the autonomous /loop. Skim top-down. Things you need to do are at the bottom.

## Loop config
- Cadence: every 60s via CronCreate `* * * * *` (session-only — dies if Claude exits)
- Sentinel: `<<autonomous-loop>>`
- Platform cadence: tick%5 → 0=opt, 1+4=Android, 2=iOS, 3=web

## Standing priorities (from your message)
1. **Parity across all 3 platforms** — Web should be ~full-featured (modulo 3D); iOS flagship; Android first-class mobile
2. **New features** — sourced from the official BoBA blog archive (`docs/blog-digest.md`, refreshed daily) and proactive web research on TCG-collecting apps + BoBA content. Every 5th tick must include research, not just first-tick research. (Discord exports dir was referenced earlier but doesn't exist in repo — blog archive is the live source.)
3. **Code optimization** — every tick%5==0 lowers complexity / removes cruft / cuts file size. PR diff should net-remove lines.

## What shipped overnight

<!-- Each tick appends a one-line summary here. Most recent on top. -->
- **tick 427 (iOS v2.343 / 605 — Find a Store summary counts locale-formatted)** — Android tick 424 parity. `summaryLine` rendered 3 counts (`shown`, `total`, `hidden`) as bare integers — full catalog hits ~2,000 stores between indie + big-box. Wrapped each in `.formatted()` so "2,130 of 2,130 authorized retailers" reads cleaner than "2130 of 2130". Same pattern as Collection counts (tick 412) + Whatnot viewer counts (tick 407).
- **tick 426 (Android — ScanReviewSheet 'Remove from queue' tooltip)** — The X icon in a list-row trailing slot can be mistaken for "close the sheet"; BOBAIconTooltip("Remove from queue") on long-press / hover clarifies the action is per-row removal. Uses tick-400's helper. Compile clean.
- **tick 425 (opt + research, −31 lines)** — Research: backlog scan reaffirms the big remaining opts (orphan DeckBuilderView delete, M5.5 Practice port, Wanted-list sharing schema) are all multi-tick or external-blocked. Recent opt ticks (415/420) found audit-only material; tick 425 finds genuine net-removable in SCRATCHPAD. Opt: collapsed the 32-line "Pickup state — 2026-05-21 (pre-compaction)" subsection into a one-paragraph summary — the bulleted shipped-work record is all in git history + WAKE_UP "Things you need to do" + the documented memory files. Net −31 lines.
- **tick 424 (Android — Find a Store count locale-formatted)** — `"${state.filteredStores.size} stores"` in PurchaseScreen → bare integer. The full catalog hits ~2,000 stores (indie + big-box); "2,130 stores" reads cleaner than "2130 stores". Wrapped in `NumberFormat.getInstance(Locale.US).format(...)` — same pattern as tick 406 viewer count + tick 414 Collection count. Compile clean.
- **tick 423 (web — Recent BoBA news empty-state shows lastUpdated stamp)** — Symmetric with tick 418's events fix. `hydrateRecentBlogPosts` previously rendered just "No recent posts." when posts was empty, dropping the lastUpdated stamp. Now renders stamp + "No recent posts. Check back as the BoBA team publishes." hint. Closes the 3-platform empty-state-with-stamp trio for blog feeds (iOS tick 422 + Android tick 421 + web 423). Also harmonizes copy across all 3 platforms ("Check back as the BoBA team publishes").
- **tick 422 (iOS v2.342 / 604 — Recent BoBA news empty-state hint)** — Mirror of Android tick 421 + iOS tick 417 events. `RecentBoBANewsSection.body` was `if posts.isEmpty { EmptyView() } else { ... }` — the whole section silently disappeared on empty days. Restructured to always render the section header + lastUpdated stamp + either the posts list OR the "No recent posts. Check back as the BoBA team publishes." hint. Closes the 3-platform empty-state coverage for both events AND blog feeds across iOS / Android / web.
- **tick 421 (Android — Recent BoBA news empty-state hint)** — Companion to tick 417 (iOS events empty-state). Android Tournament Recent BoBA news section was gated on `blogPosts.isNotEmpty()` — the whole section + header + lastUpdated stamp disappeared on empty days. Restructured to always render the header + stamp + either the post list OR a "No recent posts. Check back as the BoBA team publishes." hint. Now users see the feed IS being refreshed even when no recent posts surfaced.
- **tick 420 (opt + research — Standing-priorities note corrected, audit only)** — Research: 60+ ticks deep into polish work. Audited iOS+Android Profile sheets for dead @State patterns — all live (`discordWorking`, `roleRequestForRole`, `walkthroughsResetMsg`, `copyConfirmed` etc. all have setters + reads). StoreLocatorView clean. No new dead code found beyond the tick-415 documented orphan `DeckBuilderView` struct (~1,200 lines, deferred to dedicated session). Opt: corrected the stale "Discord exports (research/)" reference in WAKE_UP standing priorities — research/ dir doesn't exist in repo; the active source is `docs/blog-digest.md` (daily cron). Tick-420 milestone shape would normally be a bigger opt (like tick 400's BOBAIconTooltip extraction), but the orphan DeckBuilderView delete needs deliberate Xcode-Cloud verified extraction not safely done in ambient cadence.
- **tick 419 (Android — Events 'Last refreshed' stamp uses relative date)** — Companion to tick 416. Android's Tournament Events list freshness stamp at line 498-507 was rendering raw ISO (`Last refreshed 2026-05-22`); now uses `relativeOrIsoDate(stamp)` matching the blog stamp (tick 416) and iOS LearnView L2771 + web app.js L892. Closes the 3-platform freshness-stamp parity for BOTH events and blog feeds.
- **tick 418 (web — empty-state events shows lastUpdated stamp too)** — Refining tick 417's iOS empty-state fix into web parity. `hydrateTournamentEvents` previously returned early on `events.length === 0`, keeping the static "events-empty" placeholder but losing the lastUpdated stamp. Now renders stamp + empty hint when events array is empty so users see the feed IS being refreshed even when there are no upcoming events to show. iOS tick 417 (Tournament section header + stamp + empty hint) parity.
- **tick 417 (iOS v2.341 / 603 — Tournament Events empty-state parity)** — iOS `UpcomingEventsSection.body` returned `EmptyView()` when events list was empty — meaning Tournament-tab users saw NO signal that upcoming events are tracked here. Android (LearnArticleScreen L508) shows "No events scheduled yet. Check back as the BoBA team announces dates." Restructured iOS to always render the section header + lastUpdated stamp + either the events grid OR the empty-hint copy. Closes the empty-state parity gap.
- **tick 416 (Android — Recent BoBA news 'Last refreshed' stamp uses relative date)** — Android freshness stamp rendered raw ISO (`Last refreshed 2026-05-22`); iOS LearnView L2657 + web app.js L831 both use a relativeDate helper that returns "today" / "yesterday" / "Nd ago" / "Nw ago". The same helper (`relativeOrIsoDate`) already exists at line 637 for per-post BlogPostRow dates — just hadn't been threaded through the bundle.lastUpdated stamp. One-line fix. Closes the 3-platform freshness-stamp parity.
- **tick 415 (opt + research, audit-only)** — Research finding: `BOBAPlaybook/Views/Play/DeckBuilderView.swift` carries an orphan `struct DeckBuilderView: View {}` (~1,200 lines) with zero call sites in the app. Verified via `grep -rE 'DeckBuilderView' --include="*.swift"` — every reference is either inside the file itself or in stale comments. The file is still pulled into the build for shared helpers (`DeckCardRow`, `DeckManagementSheet`, `BrowserCardCell`, `BrowserCardDetailSheet`) that DecksView + CollectionCardDetailView import. Logged to AUTONOMOUS_PROGRESS.md P0 backlog as a "real opt opportunity, defer to dedicated tick" — too risky for the ambient opt cadence (~1.2K-line delete needs Xcode Cloud verification). Updated the stale "DeckBuilderView is still used" comment in DecksView.swift L23 to reflect the actual state. Net +6 / −4 lines this tick; the real opt (delete the orphan struct + extract helpers) is queued.
- **tick 414 (Android — Collection value-summary count locale-formatted, closes trio)** — iOS tick 412 + web tick 413 parity. ValueSummary's `Text(text = "$count")` cards-owned readout in CollectionScreen was a bare integer. Now wrapped in `NumberFormat.getInstance(Locale.US).format(count)` — "1,234" instead of "1234" at the headlineSmall weight. Closes the 3-platform Collection cards-owned-count locale-format trio.
- **tick 413 (web — Collection stats-bar counts locale-formatted)** — iOS tick 412 parity. `scopeCount` + `uniqueNums` in the Collection stats bar were `${scopeCount}` / `${uniqueNums}` — bare integers. Now `.toLocaleString()` — "1,234 owned · 567 unique" instead of "1234 owned". Serious collectors hit 1,000+ cards.
- **tick 412 (iOS v2.340 / 602 — Collection value-summary cards-owned count locale-formatted)** — `Text("\(count)")` at the arena(28) display size rendered as a bare integer. Serious BoBA collectors hit 1,000+ cards; "1,234" reads cleaner than "1234" at the large display weight. Wrapped in `count.formatted()` (Find tick 357 + tick 407 pattern). Audited iOS Manage Decks for Rename+Delete tooltip parity with Android tick 411 — not applicable on iOS since iOS uses `.swipeActions` instead of per-row IconButtons (different platform-native idiom; both work).
- **tick 411 (Android — Manage Decks row tooltips)** — Extended tick-400's BOBAIconTooltip helper to the per-row Rename + Delete IconButtons in DeckSecondaryScreens. Hover (mouse) or long-press (touch) reveals "Rename deck" / "Delete deck" before the destructive action fires — useful especially for the red Delete button where users hesitate. Reuses the existing helper (no new boilerplate).
- **tick 410 (opt + research, −39 lines)** — Research: scan of WAKE_UP.md found 39 lines of deep-history tick entries (ticks 290-328) — all routine polish landings, now firmly in git history and 80+ ticks old. Tick 365 already trimmed everything below tick 289. Continuing the periodic-trim discipline: dropped ticks 290-328 (39 lines), preserving the most-recent 80+ tick entries (329 → 410). WAKE_UP now 120 lines / ~90 visible ticks — comfortable scan size for Ben's morning skim.
- **tick 409 (Android — Wall truncation banner counts locale-formatted)** — Continuing tick 406's locale-format pattern. CollectionScreen + DeckWallSheet both render "Showing the first $HARD_CAP of ${entries.size} cards" — bare integers. Serious collectors hit 1,000+ cards in a Personal collection; "Showing the first 250 of 1,234 cards" reads cleaner than "of 1234 cards". Wrapped both call sites in `NumberFormat.getInstance(Locale.US).format(...)`. Compile clean. iOS + web equivalents probably exist too (queued for next tick of each platform).
- **tick 408 (web — Whatnot viewer count locale-formatted, closes trio)** — Android tick 406 + iOS tick 407 parity. `js/purchase.js` rendered `${show.viewerCount} watching` — bare integer. Now uses `.toLocaleString()` — 4-digit streams render "1,234 watching" instead of "1234". Closes the 3-platform viewer-count locale-format trio.
- **tick 407 (iOS v2.339 / 601 — Whatnot viewer count locale-formatted)** — Android tick 406 parity. UpcomingBreaksList rendered `\(show.viewerCount) watching` — bare integer. Wrapped in `.formatted()` so 4-digit streams render "1,234 watching" instead of "1234". Web port queued for next web tick.
- **tick 406 (Android — Whatnot viewer count locale-formatted)** — Purchase tile viewer count was `show.viewerCount.toString()` — bare integer. Streams routinely run into 4-digit viewer counts (1,234 reads cleaner than 1234). Wrapped in `NumberFormat.getInstance(Locale.US).format(...)` — same pattern Find uses (tick 359 results-count). Tiny user-visible polish.
- **tick 405 (opt + research, −5 lines)** — Research: continued dead-state audit started tick 395. CollectionView had `showingShareSheet` + `shareItems` declared and a corresponding `.sheet(isPresented:)` block, but `showingShareSheet = true` is never set anywhere — same pattern as the tick-395 `showingProfile` dead state. Verified `showTradeRoom` + `tradeRoomFAB` + `discord` state are NOT dead — they're gated behind DECISIONS.md #025 (Discord FAB call site commented out, implementation intact for when bot ships). Opt: dropped both @State declarations + the dead .sheet block. Net −5 lines.
- **tick 404 (Android, code style — FindScreen FQN sweep)** — 2 inline FQN call sites cleaned up: `androidx.compose.ui.platform.LocalContext.current` (line 233 in avatar block) + `androidx.compose.ui.platform.LocalHapticFeedback.current` (line 967). Added the 2 imports + body-only sed strip. Continues the established FQN-cleanup pattern (tick 350-355 etc.). Compile clean.
- **tick 403 (web — Hero States section in Rules)** — Closes the 3-platform Hero-states trio (Android tick 401 + iOS tick 402). Web Rules tab now has a separate "Hero States" list (Bench / Active / Retired / Played / Used) beneath the existing 9-item "Card Zones" reference. Same `<ul class="rules-zone-list">` markup so the existing CSS picks it up — no style work needed. The two concepts read cleanly side-by-side: zones = physical locations, states = a Hero's status in the match.
- **tick 402 (iOS v2.338 / 600 — Hero States in CardZonesSection)** — Android tick 401 split its misnamed "Card zones" into proper zones + Hero states. iOS had only the 9-zone reference, missing the Hero-states taxonomy. Refactored CardZonesSection to render two grids via a shared `zoneGrid(rows:)` helper — first the 9 physical zones, then 5 Hero states (Bench / Active / Retired / Played / Used). Same Rules-tab styling. Build 600 milestone (every 100 ticks lands a milestone-shaped polish — tick 300 archetype trim, tick 400 BOBAIconTooltip extraction, tick 402 hits build 600).
- **tick 401 (Android — Card zones content correction)** — iOS LearnView CardZonesSection (§1112) carries a canonical 9-zone rulebook reference (Hero Deck · Battle Slots · Bench/Hand · Active Battle · Discard Pile · Playbook · Hot Dog Pile · Hot Dog Discard · Abyss). Android's Rules had a section titled "Card zones" but it was actually a 5-bullet Hero-states taxonomy (Bench / Active / Retired / Played / Used) — useful but a different concept. Renamed the existing Bullets to "Hero states" + added the iOS-style "Card zones reference" Bullets below it. Both pick up `GlossaryAwareBody` rendering automatically (tick 371). New terms like "Abyss" become tap-resolvable definitions in a future glossary tick if added.
- **tick 400 (opt + research — milestone, BOBAIconTooltip extraction, −59 lines net)** — Research: 11 simple-icon `TooltipBox` call sites accumulated across ticks 384/386/389/394/396/399 — clearly past the "three similar lines" threshold from the project rule. Opt: extracted `BOBAIconTooltip(text, anchor = Below, content)` helper into `core/ui/components/BOBAIconTooltip.kt` (37 lines). Refactored FindScreen (4 sites), CollectionScreen (3 sites), DecksScreen (2 sites), CardDetailScreen (2 sites — Share + Add). Net per-site savings: ~5-7 lines × 11 sites = ~70 lines saved at call sites; helper adds 37 lines. Final net: −59 lines (4 screens, 31 insertions / 127 deletions). The 2 click-to-show sites in CardDetailScreen (formatLegalityChip + printRun) keep raw TooltipBox — they use custom `tooltipState.show()` patterns that the helper deliberately doesn't expose. Tick-400 milestone shape (tick % 100 = 0 — same milestone tradition as tick 300's archetype-block trim).
- **tick 399 (Android — TooltipBox on Card Detail Share + Add)** — Card Detail `TopAppBar` had no tooltips on Share / Add (only contentDescription for screen readers). Wrapped both in TooltipBox: "Share card" + "Add to Collection, Deck, or Show". Skipped the Back button (ArrowBack is universally understood). Closes Android Card Detail tooltip parity with the Find / Decks / Collection TopAppBars from tick 384/394/396. PlainTooltip already imported. Compile clean. The tick-400 opt slot is now well-positioned to extract a BOBAIconTooltip helper — 11+ near-identical TooltipBox sites makes it a real opt opportunity.
- **tick 398 (web — title= sweep on icon-only buttons)** — 7 more `aria-label`-only icon buttons get `title=` for desktop pointer-hover: search-clear, modal-nav-prev ("Previous card (←)"), modal-nav-next ("Next card (→)"), modal-close-btn ("Close (Esc)"), db-rules-btn, store-near-me, watch-refresh-btn, watch-player-close. Builds on tick 393's grid-density / sidebar-collapse / multiselect-clear sweep. Keyboard-shortcut hints surfaced where they exist (←/→/Esc).
- **tick 397 (iOS v2.337 / 599 — .help() on Decks toolbar)** — Decks `toolbarContent` already had `.help("Start a new deck (⌘N)")` on the iPad-sidebar New Deck button (tick 327) + `.help("Save deck …")` on the editor's save button (tick 327). Added `.help("Scan into current deck")` on the iPad-regular standalone Scan button + `.help("Filter, columns, scan, walkthrough")` on the pool overflow Menu — closing the 3-tab × 3-platform pointer-hover-tooltip matrix (Find tick 387 / Collection tick 392 / Decks today on iOS; Android tick 384/394/396 covers the other side).
- **tick 396 (Android — TooltipBox on Decks TopAppBar)** — Wraps the Decks `CenterAlignedTopAppBar` Scan + More IconButtons in TooltipBox (Find tick 384 + Collection tick 394 parity). "Scan into current deck" / "Templates, manage, rules, legality…" reveal on long-press / mouse-hover. Hit the FQN-vs-extension `PlainTooltip` issue again — same lesson as ticks 394/335 — added import + body-only sed cleaned up. Compile clean.
- **tick 395 (opt + research, −5 lines)** — Research: pattern-audit on the 9 TooltipBox call sites added ticks 379/384/386/389/394 — duplication is real but each site stays readable (per project rule against premature abstraction). Skipped a refactor. Opt: dead-state sweep on `CollectionView.swift` found `@State private var showingProfile = false` declared but never set anywhere (Profile is Find-only per `feedback_profile_only_on_find` — this was leftover before that rule). Both the @State declaration AND the `.sheet(isPresented: $showingProfile) { ProfileView() }` block dropped. Net −5 lines.
- **tick 394 (Android — TooltipBox on Collection TopAppBar)** — Wrapped all 3 Collection TopAppBar IconButtons (Scan, Filters, More) in `TooltipBox` with `PlainTooltip` content. Filters tooltip dynamically shows "Filters · N active" matching Find tick 389. iOS Collection got `.help()` at tick 392; Android now matches. Same M3 below-anchor pattern. Hit the FQN-vs-extension issue again: `PlainTooltip` is a `TooltipScope` extension, must be imported and referenced unqualified — caught at compile time. Added the import + reverted the sed mishap. Compile clean.
- **tick 393 (web — title= on icon-only buttons)** — Sweep of icon-only buttons that had `aria-label` but no `title=` — screen readers heard the affordance, but mouse-hover users saw nothing. Added title= on: Find grid-density S/M/L picker (1-char labels are visual not informational), sidebar-collapse-toggle (desktop-only), multiselect-clear (X icon). Skipped multiselect-wall + collection-wall-btn (both carry visible "Wall" text already). iOS .help() across Find + Collection toolbars; web matches per icon-only button.
- **tick 392 (iOS v2.336 / 598 — .help() tooltips on Collection toolbar)** — Find toolbar got .help() at tick 387; Collection had none. Added on all 3 Collection toolbar buttons: Filters (dynamic " · N active" when filters are on, matching the tick-387 Find Filters), Scan (iPad-regular only, "Scan into {Designation}"), and overflow Menu ("Display mode + collection actions"). iPad trackpad / Magic Keyboard / Mac Catalyst pointer-hover now reveals affordance hints across both Find and Collection toolbars.
- **tick 391 (Android — drop duplicate broken "Scan into deck" overflow item)** — Bug + opt: the Decks overflow Menu carried a "Scan into deck" item whose onClick was `{ menuOpen = false /* opens scan modal via parent */ }` — i.e., a no-op stub. The TopAppBar Scan IconButton already wires the real scan action via `onScanClick`. Dropping the broken duplicate matches the existing pattern noted in the file ("duplicate top-bar Edit button was confusing — there's now exactly one way to open the editor"). Same one-way-in discipline. Net −1 line (replaced 5-line broken item with 4-line provenance comment that future devs can also drop later).
- **tick 390 (opt + research, −39 lines)** — Research: blog-digest unchanged since tick 360 scan; CI green for shipped commits; Stage B's tick-361 Mirror sync awaits tomorrow's 04:00 UTC nightly for verification. Opt: dead-CSS sweep on `css/styles.css`. Found 6 unused classes (`.browse-sport-athletes` explicitly noted as "replaced by hero chips", and the 5-class `.collect-treatment-*` block superseded by the post-2026-04-24 taxonomy split that introduced `.treatment-*` without the `collect-` prefix). Verified zero references in index.html or any js/ file (and zero dynamic concatenation, unlike the `desig-*` classes which ARE built via template literal in collection.js). Net −39 lines.
- **tick 389 (Android — Filters tooltip shows active count)** — iOS tick 387 + web tick 388 parity. Find SearchBar's Filters TooltipBox previously read "Filters" plainly; now reads "Filters · N active" when `state.activeFilterCount > 0`. Mouse-hover / long-press users see the count without opening the sheet (the BadgedBox dot is also visible, but the textual hint reinforces). Closes the 3-platform Filters-active-count-tooltip trio.
- **tick 388 (web — Filter toggle tooltip with active count)** — iOS tick 387 .help("Filters · N active") parity on web. Added static `title="Filters"` to the filter-toggle-btn in index.html plus dynamic update in `updateFilterBadge()` that swaps to `"Filters · N active"` when filters are on. Desktop pointer-hover now reveals the count without opening the panel. Closes the 3-platform Filters-tooltip trio with Android tick 384 + iOS tick 387.
- **tick 387 (iOS v2.335 / 597 — .help() tooltips on Find toolbar)** — Android tick 384+386 wired TooltipBox on all 4 Find IconButtons; iOS Find toolbar had .help() only on the Surprise menu item. Added .help() on Profile, Scan, and Filters toolbar buttons (Filters tooltip dynamically shows " · N active" when filters are on, matching the existing badge dot). Pointer-hover (iPad trackpad, Magic Keyboard, Mac Catalyst) now reveals affordance hints before firing the action. Touch users ignore it (no pointer). Closes the 3-platform toolbar-tooltip parity for Find.
- **tick 386 (Android — TooltipBox on Find Profile + Overflow icons)** — Tick 384 wired TooltipBox on Filters + Scan. This tick rounds out the Find TopAppBar: Profile icon (leading) + Overflow icon (trailing) now also show long-press / hover tooltips. Same M3 PlainTooltip + below-anchor pattern. Closes the parity of "every Find icon button has an affordance hint" — iOS .help() + web title= already cover all of these.
- **tick 385 (opt + research, −4 lines)** — Research: verified the 10 cross-platform glossary terms (Hero Deck · Sudden Death · Top Deck · Laundry Phase · Apex · Brawl · Tecmo Bowl · Spec 160 · High Ball · Low Ball) are present on iOS gameTerms (10/10), Android LearnCorpus (10/10), web data-term buttons (10/10). 3-platform lockstep confirmed. Opt: trimmed 2 stale provenance comments in iOS FilterSheetView — "Moved into the filter sheet (2026-04-22)" (3 lines, structure self-documents now) + "Weapons (renamed from Element 2026-04-23…)" (3 lines, replaced by 2-line anchor to DECISIONS.md #027 — the canonical source for the rename). Net −4 lines.
- **tick 384 (Android — tooltips on Find Filters + Scan icons)** — Wrapped both M3 `SearchBar` trailing IconButtons in `TooltipBox` so Chromebook + tablet users (mouse hover OR long-press on touch) see "Filters" / "Scan a card" labels without firing the action. iOS uses `.help()` + web uses `title=` for the same purpose; Android M3's TooltipBox is the platform-native equivalent. Touch users on phone retain tap-to-fire — TooltipBox only adds the long-press tooltip; the IconButton's onClick is unchanged. Same M3 PlainTooltip primitive as the FormatLegalityChip + tick-379 print-run chip use. Compile clean.
- **tick 383 (web — print-run chip tap-to-explain popover)** — Android tick 379 + iOS tick 382 parity. The /5 · /10 · /25 · /50 · SSP · Serial chips in the Treatment stat cell are now `<button class="print-run-chip">` instead of `<span>`. Hover gives a native `title=` tooltip (instant on desktop); click routes through `bobaShowPopoverMenu` with the same per-label explainer text iOS + Android ship (DECISIONS.md #028 weapon-tied print runs). Added `:hover { filter: brightness(1.12) }` + `:focus-visible` outline so the chip reads as interactive. Closes the 3-platform print-run-explainer trio.
- **tick 382 (iOS v2.334 / 596 — print-run chip tap-to-explain popover)** — Android tick 379 parity. iOS print-run / SSP chip on Card Detail now wraps in a Button + popover — tapping reveals the BoBA Inspired Ink convention (Hex /5 · Glow /10 · Fire /25 · Ice /50 per DECISIONS.md #028) plus SSP and Serial-fallback meanings. `.popover` with `.presentationCompactAdaptation(.popover)` so it stays a small popover even on iPhone compact (instead of adapting to a full sheet — too heavy for a 7-char chip). Also added `.help()` + `.accessibilityHint()` for iPad/Mac hover + VoiceOver. Web port queued for next web tick.
- **tick 381 (Android, code style — DeckEditorSheet FQN sweep)** — 8 inline FQN call sites cleaned up at lines 147-149, 381, 539, 544, 552, 557 (OutlinedButton 3× · Surface · LocalContext · produceState · rememberCoroutineScope · LocalHapticFeedback). Added 5 new imports (OutlinedButton, produceState, rememberCoroutineScope, LocalContext, LocalHapticFeedback) + body-only sed strip pattern. Continues the FQN-cleanup pattern from ticks 350-355. Compile clean.
- **tick 380 (opt + research, −3 lines)** — Research: catalog scan surfaced 1,304 Inspired Ink cards across 9 weapon types — confirms ~579 fall into the "Serial" fallback bucket (Steel/Gum/Brawl/Super/Alt — no public print run per DECISIONS.md #028); tick 379's tooltip explanation correctly disambiguates them. Glossary cross-platform state in lockstep: iOS gameTerms = Android LearnCorpus.glossaryGame = web glossary buttons, with the 7 tick-372 + 3 tick-377 additions verbatim. Opt: trimmed the 5-line "Replaced 2026-04-27 per bobaleagues handoff §7 design briefs" provenance block in DeckBuilderStore.swift to a single sentence — git history captures the change-of-archetypes, the 5-line block was the kind of "rot when codebase evolves" comment the project rules warn against. Also corrected stale "we have none yet" claim about non-letter-edged glossary terms in LearnView (the iOS regex IS exercising the lookaround path now for terms like "G&S", "F/S", "Chillin' / Grillen"). Net −3 lines.
- **tick 379 (Android — print-run chip explains itself on tap)** — The /5 · /10 · /25 · /50 · SSP · Serial chips on card detail (tick 189) carried no inline explanation of what the labels meant. Casual users don't know the BoBA Inspired Ink convention (Hex /5 · Glow /10 · Fire /25 · Ice /50 per DECISIONS.md #028). Wrapped the chip in `TooltipBox` (same M3 pattern as the FormatLegalityChip above it) with per-label explainer text: tapping the chip reveals the print-run meaning + weapon binding. Closes a real "what does /25 mean here?" gap. iOS + web ports queued. Compile clean.
- **tick 378 (web — 3 Madness terms close 3-platform trio)** — Added Spec 160 · High Ball · Low Ball to the web glossary buttons (Android tick 376 + iOS tick 377 parity). Total glossary entries on web now 48. Verbatim definitions across all 3 platforms. Closes the Madness-terms trio.
- **tick 377 (iOS v2.333 / 595 — plural matching + 3 Madness terms)** — Two-part catch-up: (1) `boba_glossaryAware` regex now allows `\b{escaped}s?\b` for letter-edged terms whose singular doesn't end in 's' (web tick 373 + Android tick 374 parity — closes the 3-platform plural-matching trio). "Hot Dogs" / "Hero Decks" / "Top Decks" / "Bonus Plays" tap-resolve to their singular dictionary terms. (2) Added Spec 160 · High Ball · Low Ball to `gameTerms` (Android tick 376 parity — closes the 3-platform Madness-terms trio). Inline-glossary aliases for Substitute / Substitution can stay; they map distinct alias forms that don't pluralize cleanly through `s?`.
- **tick 376 (Android — 3 Madness-format glossary terms)** — Added Spec 160 · High Ball · Low Ball to `LearnCorpus.glossaryGame`. Tick 364 surfaced these in the Madness team-play bullets and tick 371 wired bullets through `GlossaryAwareBody`, but the regex had no entries to bite on. Now each term tap-defines inline. iOS + web ports queued.
- **tick 375 (opt + research, audit-only)** — Research: CI fully green across all 21 recent runs (every commit since tick 354 successful on Android · build + pages). Stage B's tick-361 Mirror sync fix is queued for tomorrow's 04:00 UTC cron — can't verify until then. 3-platform glossary state in lockstep: iOS gameTerms (23 game + 14 trading) = Android LearnCorpus = web index.html glossary buttons, with the 7 tick-372 additions verbatim. Plural-matching shipped on web (373) + Android (374); iOS port pending tick 377. Opt: updated AUTONOMOUS_PROGRESS.md L6 — was claiming "every loop tick reads + updates it" but since tick 202 the active log has been WAKE_UP.md. Now correctly framed as the pre-compaction archive. Net: roughly line-neutral; the rewording is honest provenance, not lines for line's sake.
- **tick 374 (Android — inline glossary matches plurals)** — `detectGlossaryHits` regex extended to `\b{esc}s?\b` for letter-edged terms whose singular doesn't already end in 's'. Web tick 373 parity — "Hot Dogs" / "Hero Decks" / "Top Decks" / "Bonus Plays" tap-resolve to the singular dictionary term. Terms ending in 's' (e.g. "comps") keep the strict boundary so "comp" doesn't accidentally trigger the comps definition. Compile clean. iOS port queued for tick 377.
- **tick 373 (web — inline glossary matches plurals too)** — `wireInlineGlossary` regex used `\b{term}\b` for letter-edged terms, so "Hot Dogs" / "Hero Decks" / "Top Decks" / "Bonus Plays" never tap-resolved despite their singulars being in the glossary. Extended the alternation to `\b{term}s?\b` plus a fallback lookup that strips the trailing `s` to find the singular glossary entry. Display form preserves the matched plural; only the lookup is depluralized. Particularly visible in tick 358's Madness + Double-Up sections where "Hot Dogs" / "Foil Hot Dogs" / "Heroes" / "Plays" were everywhere. Single-letter risk acceptable — "Apexs" / "Brawls" don't appear in real BoBA prose, so false-positive risk is theoretical. Node-syntax-validated.
- **tick 372 (iOS v2.332 / 594 — 7 glossary terms close 3-platform trio)** — Added Hero Deck · Sudden Death · Top Deck · Laundry Phase · Apex · Brawl · Tecmo Bowl to `GlossaryView.gameTerms`. `boba_inlineGlossary` derives from `gameTerms` (tick 257 hoist), so the new terms picked up automatically in both the Glossary tab AND every `GlossaryAwareText` render across LearnView (including tick 367's DoubleUp + Madness sections). Web tick 368 + Android tick 369 already shipped these — closes the 3-platform glossary trio with verbatim definitions across all 3.
- **tick 371 (Android — bullet items now glossary-aware)** — `LearnSection.Bullets` rendering used plain `Text(item, ...)`; only `LearnSection.Body` ran through `GlossaryAwareBody`. Every bullet in Double-Up rules, Penalty Reference, Nationals divisions, Setup match-flow etc. had glossary-worthy terms that didn't tap-to-define. Refactored `GlossaryAwareBody` to accept optional `style` + `modifier` (defaults preserve the standalone-body shape, so the existing call site is unchanged). The bullet renderer now calls the same primitive — same `allGlossaryTerms()` / `detectGlossaryHits()` / `LinkAnnotation.Clickable` pipeline, just hosted inside the Row. Closes a real consistency gap: same term, different markup, different affordance. Compile clean.
- **tick 370 (opt + research, −3 lines)** — Research: blog-digest scan confirms previously-mined posts still match current app state — no new actionable items surfaced. One discrepancy noted: 2025-12-18 "Brawl Brackets for 2026 National" blog cites $75K total prize pool (up to $35K cash); our Nationals divisions show Brawl at $20K. Likely superseded by later official announcement (the per-division figures sum to ~$500K matching the 2026-02-05 prize-pool reveal) but couldn't verify in one tick — skipped to avoid introducing wrong data. Opt: trimmed stale "Started: 2026-05-21 (post-compaction)" + "Resuming tick numbering at 202" lines from WAKE_UP Loop config block. We're 170 ticks past that point; both lines were just historical noise on every wake-up scan now. Net −3 lines.
- **tick 369 (Android — same 7 glossary terms shipped on web tick 368)** — Added Hero Deck · Sudden Death · Top Deck · Laundry Phase · Apex · Brawl · Tecmo Bowl to `LearnCorpus.glossaryGame`. Picks up automatically in two places (zero extra wiring): the Glossary tab list AND every `GlossaryAwareBody` render inside Rules / Strategy / Collect / Tournament articles (Android tick 184 patterns). Same definitions as web for cross-platform consistency. iOS port queued for tick 372.
- **tick 368 (web — 7 glossary entries for tournament + game-mechanic terms)** — Tick 363 extended `wireInlineGlossary` to `<td>` cells, but a check of the matched-terms showed the actual regex had nothing to bite on inside the Tournament Double-Up + Madness + Nationals sections — none of those game-mechanic / division terms were in the glossary. Added 7: **Hero Deck · Sudden Death · Top Deck · Laundry Phase · Apex · Brawl · Tecmo Bowl**. Now tapping any of those in Tournament prose (or anywhere else in Rules / Strategy / Collecting / Tournament panels) opens the definition popover. Closes the parity loop iOS tick 367 implied — both platforms now have the same surface coverage. iOS+Android will inherit via subsequent ticks once their glossary lists pick these up.
- **tick 367 (iOS v2.331 / 593 — DoubleUp + Madness glossary wiring)** — Both sections were using plain `Text(...)` for body + bullet content; glossary-worthy terms (Hot Dogs · Hero Deck · Top Deck · Sudden Death · Apex deck · Foil Hot Dogs · Spec 160 · Granny's Gum · Brawl · Tecmo Bowl · High Ball · Low Ball · Substitution) weren't tappable. Wrapped via `GlossaryAwareText` (same primitive 6+ other sections already use) so those terms now tap-to-define inline. Web tick 363 made the equivalent change there (extended selector to `<td>` cells). Same `boba_glossaryAware` underlying — no new glossary entries needed, the regex coverage is already complete.
- **tick 366 (Android — Penalty Reference 5-level structure parity)** — Android's Tournament Learn Penalty Reference was 6 ad-hoc infraction examples (slow play, marked card, etc.); iOS PenaltyReferenceSection + web index.html:2092 both use the canonical 5-level penalty TYPES (Caution → Warning → Game Loss → Match Loss → Disqualification), with infraction examples slotted under them. Restructured to match: 5 ordered levels in a Bullets block, plus a "Common infractions" Bullets block beneath that maps specific infractions to the matching penalty level. Compile clean.
- **tick 365 (opt + research, −88 lines)** — Research: re-scanned `docs/blog-digest.md`; the only recent action item left (Tecmo Bowl Double-Mega 16-pack/160-card claim from 2026-02-18) is already correctly captured in `assets/data/sealed_products.json` (`packsPerBox: 16, totalCards: 160` on the SEALED-tecmo-double-mega-box entry). No new content work surfaced. Opt: trimmed 88 deep-history tick entries (tick 289 down to tick 202) from WAKE_UP.md — they're all preserved in git history + the corresponding code is already shipped. WAKE_UP now shows 75 most-recent ticks (365 → 290), about 4 days of context — plenty for "what shipped overnight" without 200+ lines of pre-compaction state Ben no longer needs to scan. 204 lines → 116 lines.
- **tick 364 (Android — Tournament Learn content depth parity)** — Android's LearnContent had Double-Up + Madness as 1-paragraph Bodies; iOS DoubleUpSection (5 specific rules bullets) + web tick 358 ship the full depth. Upgraded: Double-Up now has the "backgammon doubling cube" intro hook + 5-bullet rules list (Press / Fold / Top Deck tie / Laundry Phase). Madness now has the 4-Coach team framing + 2 distinct variants (Apex+AlphaTrilogy Madness · HiLo Madness) with the same depth iOS+web have. Same `LearnSection.Body` + `LearnSection.Bullets` primitives — no new section type needed. Compile clean.
- **tick 363 (web — inline glossary now wires `<td>` cells too)** — `wireInlineGlossary` selector previously matched only `p` + `li` inside the Rules / Strategy / Collecting / Tournament panels. Tournament's Madness table (tick 358) + Nationals divisions table (tick 313) + Match Structure table all carry glossary-worthy terms (Hot Dogs · Apex deck · Foil Hot Dogs · Spec 160 · Brawl · Tecmo Bowl · Sudden Death etc.) inside `<td>` cells that were bare. Extended the selector to include `td`; same term, same markup-class, now consistently tappable across paragraph + list + table prose. Skipped `<th>` (column headers — no glossary-worthy text). Node-syntax-validated.
- **tick 362 (iOS v2.330 / 592 — Decks sidebar pull-to-refresh)** — `.refreshable` on the saved-decks `List` in the iPad sidebar. Web tick 14 added an explicit Refresh button; iOS uses the native pull-to-refresh idiom (drag the sidebar list down → system spinner → `await store.loadSavedDecks()`). Gated on `auth.isAuthenticated` — anonymous users see the sign-in CTA instead and gain nothing from a refresh. iPhone compact width has no sidebar; the existing onChange-of-auth + first-task-load path already covers that case.
- **tick 361 (CI fix — Stage B nightly red on Mirror drift)** — `pipeline · stage B · recognize` workflow has been failing nightly since tick 197 (iOS Card.swift gained `printRunLabel` on 2026-05-21 but `pipeline/recognition/CardRecognitionCLI/Sources/CardRecognitionCLI/Mirror/Card.swift` was never resynced via `sync_mirror.sh`). Ran the sync; mirror picked up the 20-line `printRunLabel` accessor. CI green expected on next nightly trigger. Per loop guidance "CI went red" trumps platform cadence — opted to land this instead of Android polish. Cron handles tick 362 (iOS).
- **tick 360 (opt + research, −10 lines)** — Research: scanned `docs/blog-digest.md` (45 posts, last refreshed 2026-05-21). Most-recent posts (2026-05-20 Open Letter to Live Sellers · 2026-05-19 Ripped Restock retail program · 2026-05-15 $150K for St. Jude) surface in iOS/web/Android Tournament tab via `blog-feed.json` automatically — no new content work needed. The historical posts that did need extraction (Set Ascension explainer, DBS rebalance callout, Checklist Format) are already mined into Learn articles + glossary entries across all 3 platforms. Opt: trimmed the stale `Loop stopped at tick 201` postmortem in AUTONOMOUS_PROGRESS.md — listed punch-list items #3 + #4 + #8 as "still pending" but all 3 have since closed across 3 platforms (verified via grep). Net −10 lines.
- **tick 359 (Android — Find results-count label closes 3-platform trio)** — ResultsHeader now suffixes the active non-default sort label (iOS v2.329 + web tick 353 parity). New `sortLabel: String? = null` parameter; FindContent passes `state.sortOrder.label` when it's not DEFAULT. Also locale-formats the count via existing `NumberFormat.getInstance(Locale.US)` helper. `12,345 cards · Recently Added` shape matches all 3 platforms now. `remember(count, sortLabel)` keys the format string so repeated renders don't reformat the same number.
- **tick 358 (web — Tournament tab: Double-Up + Madness content sections)** — Web previously had only a 1-line glossary entry for Double-Up and zero coverage of Madness team formats. iOS DoubleUpSection (LearnView §3040-3068) + MadnessSection (§3074-3106) + Android LearnContent (§710-723) were fully shipped; this closes the 3-platform Tournament-content trio. Renders between CHECKLIST FORMAT and 2026 NATIONALS DIVISIONS, matching iOS section order. Double-Up: 5-bullet rules list (Press / Fold / Laundry Phase). Madness: 2-row table (Apex+AlphaTrilogy Madness · HiLo Madness) using the existing `.rules-table` styling. Both reuse existing CSS — no new styles.
- **tick 357 (iOS v2.329 / 591 — Find results-count label)** — Active non-default sort now appended to the results-count label: `12,345 cards · Recently Added`. Web tick 353 + Android tick 354 parity (web does the same, Android could follow on a future tick to read the active SortOrder). Also locale-formats the count via `count.formatted()` to add the thousands separator. Default sort = unchanged "(N) cards" — no chrome added when there's nothing extra to communicate.
- **tick 356 (Android — DeckSummaryBar empty-state parity)** — iOS DecksView.swift:1648/1691 parity: when draft is brand-new (zero cards AND default "New Deck" name), the bar swaps to "Build a deck / Tap to open the editor" and suppresses the counts, STANDARD format pill, and the (always-passing) Legal chip. Before this an empty draft showed "New Deck · 0/8 H · 0/30 P · 0 BP · 0/10 HD · STANDARD · Legal" which buried the CTA. Threshold matches iOS verbatim — same `hasDraft` rule (`cards.count > 0 || deckName != "New Deck"`).
- **tick 355 (opt — CustomRainbowEditorSheet FQN sweep)** — 9 inline FQN call sites cleaned up at lines 216-232 (rememberSavedField + rememberSavedFieldBool + rememberSavedSet helpers): `androidx.compose.runtime.MutableState` · `androidx.compose.runtime.saveable.rememberSaveable` · `androidx.compose.runtime.saveable.Saver` · `androidx.compose.runtime.mutableStateOf`. Added 3 imports (MutableState, Saver, rememberSaveable) + body-only sed strip pattern. Compile clean. Net -0 lines (replacement only).
- **tick 354 (Android)** — Added `RECENTLY_ADDED` to SortOrder enum (closes 3-platform sort trio with iOS v2.328 + web tick 283). Catalog list passed into applySort + lazy Map<bobaId, Int> built only for the RECENTLY_ADDED case so the other 9 sort orders pay no extra cost. Sort by reverse catalog index — newer sets append, so recently-added cards bubble up.
- **tick 353 (web)** — Active non-default sort surfaced in the results-count label: `12,345 cards · Recently Added`. Reads the dropdown's option label (already human-friendly) so users see what's ordering their results without opening the dropdown. No-op label appended when sort is default.
- **tick 352 (iOS v2.328 / 590)** — Added `Recently Added` sort option to iOS Find (web tick 283 parity, closing the 3-platform sort trio — iOS already had Recently Added shelf at tick 282, now also has it as a sort). Implementation: new `case recentlyAdded` on CardSortOrder enum + sort by reverse catalog index via a new O(1) `catalogOrderById` Dictionary built alongside the existing id→Card index in `rebuildIdIndexes()`.
- **tick 351 (Android, code style)** — CollectionScreen 8 more FQN call sites swept: AlertDialog · Badge · BadgedBox · FilterChip · pulltorefresh.PullToRefreshBox · RadioButton · TextButton · Surface (multiple). Added all to imports + body-only sed. Continues tick 350's CollectionScreen pattern (which only covered the runtime + HorizontalDivider FQNs).
- **tick 350 (opt, milestone — CollectionScreen FQN sweep)** — 10+ inline FQN call sites in CollectionScreen cleaned up: rememberCoroutineScope (2x), LaunchedEffect (1x), HorizontalDivider (4x). Each was already imported. Material3.BadgedBox / Badge / FilterChip / pulltorefresh.PullToRefreshBox still pending (need new imports). Sed-style body-only replacement (`/^import /!s|FQN|short|g`) prevents the greedy-match bug from ticks 330+341. Net -0 lines (replacement only).
- **tick 349 (Android, audit + cleanup)** — Audited Android Glossary for search filter port from web tick 348 / iOS v2.327 — turns out Android already had it shipped (Glossary search at LearnArticleScreen L758, with substring filter against both term + definition). Android beat iOS to this one. Used the tick to clean up 4 inline FQN call sites (rememberSaveable / mutableStateOf / OutlinedTextField / rememberModalBottomSheetState) → top-of-file imports. Net -7 lines on collapsed state-init blocks.
- **tick 348 (web)** — Glossary search input on web (iOS v2.327 parity). Substring filter against term + definition; sections hide when filtered to zero; inline empty-state message when both Game + Trading lists go to zero. Native `<input type="search">`. Same data shape as inline-glossary wiring — both read `data-term`/`data-def` from the rows.
- **tick 347 (iOS v2.327 / 589)** — Added `.searchable` to iOS Glossary corpus (38 terms total). Substring match against term + definition, case-insensitive. Sections auto-hide when filtered to zero. Empty-search state shows "No glossary terms match" + Clear-search Button. iOS 26 native search docking (navigationBarDrawer always-visible). Type "htd" → instantly find Home Team Discount.
- **tick 346 (Android, code style)** — WatchPage: 5 inline FQN call sites collapsed via imports (rememberCoroutineScope · remember · mutableStateOf · rememberSaveable). Each was wrapping a 1-line State init in 3 lines; the cleanup also reformatted to one-line idiomatic form (`var x by remember { mutableStateOf(...) }`), net -6 lines.
- **tick 345 (opt, code style)** — DecksScreen: 3 more FQN call sites → top-of-file imports (rememberCoroutineScope · LocalContext · LocalHapticFeedback). Continues the FQN-cleanup pattern from ticks 321/325/330/336/339/341 — every file with inline `androidx.compose.X.Y` references gets the import treatment when touched.
- **tick 344 (Android)** — "🎲 Surprise me from all cards" secondary CTA on Find empty state. Closes 3-platform trio (iOS v2.326 + web tick 343). BOBAEmptyState already supported secondaryActionLabel/onSecondaryAction (tick 214). Clears filters via FindEvent.ClearAllFilters, then findActions.requestSurprise() routes through the existing LaunchedEffect collector which picks from the freshly-recomputed results.
- **tick 343 (web)** — "🎲 Surprise me from all cards" empty-state CTA on Find (iOS v2.326 parity). resetFilters() then requestAnimationFrame → fireSurpriseMe so filteredCards has rebuilt before the random pick. Closes 3-platform empty-state-Surprise trio's web leg.
- **tick 342 (iOS v2.326 / 588)** — iOS Find empty-state now offers a secondary "Surprise me from all cards" CTA below "Clear All Filters". Clears filters then fires Surprise on the next runloop (async dispatch so filteredCards recomputes first). Two clicks collapsed into one — useful when user has filtered down to zero and would benefit from a discovery jump rather than fighting the filter.
- **tick 341 (Android, code style)** — FindScreen: cleaned up 2 missed FQNs from tick 321 (LaunchedEffect call sites at lines 129+135) + 2 HorizontalDivider FQN call sites in the overflow Menu. Both added as proper imports.
- **tick 340 (opt, −8 lines)** — Trimmed obsolete "Pending iOS asks for tick 192" section from AUTONOMOUS_PROGRESS. Both items (triple-spinner collapse + non-blocking pricing refresh) shipped tick 192 v2.293 / build 555. The queued-asks header was already provenance, not active backlog.
- **tick 339 (Android, code style)** — ProfileSheet `LocalConfiguration` inline FQN → top-of-file import + short reference. Continues the tick 321/325/330/336 FQN-cleanup pattern. Net 0 lines (added 1 import, collapsed 1 line of inline FQN).
- **tick 338 (CI fix, urgent)** — Android CI red again (tick 334 cheat sheet). `LocalConfiguration.current` is a @Composable getter; was being read inside the LazyListScope DSL builder (NOT a Composable context). Hoisted the read out of LazyColumn to the enclosing Composable function scope and captured `isPhoneOnlyTouch` in the closure. Should green up next run.
- **tick 337 (iOS v2.325 / 587)** — Keyboard shortcuts cheat sheet on iOS Profile (web tick 333 + Android tick 334 parity). 6 rows with proper Apple glyphs (⌘ + ⇧ + ← / →). Only renders on `horizontalSizeClass == .regular` (iPad) since iPhone has no keyboard. Accessibility label spells out "Command R" etc. so VoiceOver reads usable text.
- **tick 336 (Android, code style)** — CardDetailScreen DisposableEffect FQN → short import (continues tick 321/325/330 pattern). Defensive cleanup after tick 335 CI red on similar FQN issue (asSharedFlow — extension functions don't bind via FQN receiver). Verified compile-time difference: DisposableEffect is a regular Composable so FQN works, but consistency with LaunchedEffect import means call sites stay readable.
- **tick 335 (CI fix, urgent)** — Android CI red since tick 329 — `asSharedFlow` was called on the new MutableSharedFlow without importing it (used FQN for MutableSharedFlow + SharedFlow inline, but Kotlin can't resolve the extension function `.asSharedFlow()` without the import). Added `asSharedFlow` import + replaced inline FQNs with the now-imported short names. Ticks 329, 330, 331, 334 all blocked until this lands.
- **tick 334 (Android)** — Keyboard shortcuts cheat sheet ported to Android Profile sheet (web tick 333 parity). 6 rows: Ctrl+1..5 tabs · `/` Find · `R` Surprise · `N` Clear deck · Ctrl+S Save · Ctrl+←/→ Card detail nav. Hidden on devices with no hardware keyboard (`Configuration.KEYBOARD_NOKEYS`) so phone users don't see Chromebook-only info.
- **tick 333 (web)** — Added "Keyboard shortcuts" cheat sheet to web Profile sheet (6 rows: `/` focus search, `R` Surprise, `N` Clear deck, `⌘/Ctrl+S` Save deck, `← →` modal nav, `Esc` close). Surfaces all shortcuts shipped this session in one discoverable spot — alternative to per-button tooltips users have to hunt for. Hidden on mobile touch viewports (no physical keyboard) via `@media (max-width: 768px) and (pointer: coarse)`.
- **tick 332 (iOS v2.324 / 586)** — Added `.help("Pick a random card · ⌘⇧R")` tooltip to the Surprise Me menu item in Find. Pointer hover on iPad / Mac Catalyst surfaces the keyboard shortcut. Web has the same affordance via title attribute (tick 318); iOS tick 327 covered Decks SAVE/New. Now extends to Find's Surprise.
- **tick 331 (Android, fix tick 329)** — Ctrl+→ / Ctrl+← keystroke now only consumed by the root key handler when CardDetailScreen is in composition. Without this, typing Ctrl+→ in Find's search field would silently lose the OS "next-word" cursor-movement to a no-op nav request. New `isOnDetail: StateFlow<Boolean>` on CardNavigationStore; CardDetailScreen DisposableEffect flips it true on mount / false on unmount; root handler checks before consuming.
- **tick 330 (opt, code style)** — CardDetailScreen: replaced 2 inline `androidx.compose.runtime.LaunchedEffect(...)` FQN call sites with top-of-file import + short reference. Pattern continues from tick 321 (FindScreen) + tick 325 (DecksScreen) — every file with multiple LaunchedEffect FQN call sites gets the same canonical import treatment.
- **tick 329 (Android, Card Detail keyboard)** — Ctrl+→ / Ctrl+← walks Card Detail siblings (iOS Cmd+arrow tick 287 + web ArrowLeft/Right parity). Extended CardNavigationStore singleton with `requestAdvance: SharedFlow<Int>` + `advance(delta)`. MainActivity injects + passes through BOBAApp; root key handler emits on Ctrl+arrow. CardDetailScreen LaunchedEffect collects + applies the existing wrap-around math. Closes the 3-platform card-detail-keyboard-nav trio.

## Things you (Ben) need to do

<!-- Anything that needs human action lands here. -->

### Still queued from before the loop started
1. **Manual-upload `app-release.aab`** (32 MB at `android/app/build/outputs/bundle/release/app-release.aab`) → Play Console → Internal Testing → Create new release.
2. **Walk through Play Console required forms** using `android/distribution/play-console-listing.md`.
3. **Upload icon + feature graphic + 5 screenshots** from `tools/play-assets/out/`.
4. **Re-capture screenshots 2–5** by booting the Pixel 9 Pro AVD from Android Studio's Device Manager + running `bash tools/play-assets/capture-screens.sh`.
5. **Add internal-testing emails**, share opt-in URL.
6. **Promote to Open Testing** track when ready.

## How to stop the loop

```sh
# In Claude Code:
/cron list
/cron delete <id>
```

Or just exit Claude — session-only cron dies automatically.
