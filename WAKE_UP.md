# Good morning! Overnight loop summary

> Updated by the autonomous /loop. Skim top-down. Things you need to do are at the bottom.

## Loop config
- Cadence: every 60s via CronCreate `* * * * *` (session-only — dies if Claude exits)
- Sentinel: `<<autonomous-loop>>`
- Platform cadence: tick%5 → 0=opt, 1+4=Android, 2=iOS, 3=web

## Standing priorities (from your message)
1. **Parity across all 3 platforms** — Web should be ~full-featured (modulo 3D); iOS flagship; Android first-class mobile
2. **New features** — sourced from the official BoBA blog archive (`docs/blog-digest.md`, refreshed daily) and proactive web research on TCG-collecting apps + BoBA content. Every 5th tick must include research, not just first-tick research. (Discord exports dir was referenced earlier but doesn't exist in repo — blog archive is the live source.)
3. **Code optimization** — every tick%5==0 lowers complexity / removes cruft / cuts file size. PR diff should net-remove lines.

## What shipped overnight

<!-- Each tick appends a one-line summary here. Most recent on top. -->
- **tick 469 (Android — lower format-legality gate to 80dp + wire Other Versions)** — Audit during tick: Android polish surface is largely complete after the print-run + format-legality rollouts. Two consistency fixes: (a) format-legality gate was 100dp because "abbrev needs more room"; at 80dp wide cell "S+ C" still fits comfortably + matches print-run's 80dp gate for consistency. Lowered to 80dp. (b) CollectionCardDetailScreen "Other versions" LazyRow at 80dp tiles now also passes `formatLegalityHint` — previously skipped because the 100dp gate would have filtered. With the lower gate, restricted variants now surface their legality status alongside the print-run label on the Other-Versions strip (e.g., a /5 Inspired Ink of an over-160-power hero shows BOTH /5 in cyan top-trailing AND "S+ C" in amber bottom-leading).
- **tick 468 (web — format-legality hint badge, closes 3-platform trio for Discord backlog #4)** — Closes the per-cell half of Discord backlog #4 (Card Detail chip strip closed earlier ticks 179/203). Added `restrictedLegalAbbrevFor(card)` to js/app.js mirroring the iOS + Android helpers (returns "S+ C"-style string when legal in less than 4 formats, else null). New `.format-legality-hint` `<span>` overlay in `buildCardElement` positioned bottom-LEADING via CSS. Same amber palette (`var(--boba-orange)`) as iOS + Android. Container query `@container card-cell (max-width: 130px)` hides at 'S' density (stricter than the print-run gate's behavior since abbrev text needs more room). Discord backlog now fully closed across all 3 platforms — the only remaining open Discord item is #6 (Wanted-list public sharing, multi-week schema).
- **tick 467 (iOS v2.351 / 613 — format-legality hint badge, Android tick 464/466 parity)** — Direct 2-platform parity port. Added `static func restrictedLegalAbbrev(for card: Card) -> String?` to CardFormatEligibility.swift mirroring Android's helper: returns abbreviation of legal formats ("S+ C") when card is legal in less than 4, else nil. CONSTRAINED treated as legal. Added `formatLegalityHintBadge` `@ViewBuilder` on BOBACardCell (Design.swift) + new `.overlay(alignment: .bottomLeading)` in the body. Same `.full`-only gate as printRunBadge — small thumb call sites (scan chips, suggestion thumbnails) skip both badges automatically. Amber `Design.Colors.bobaOrange` text matches Android. Because BOBACardCell-via-BOBACardGridItem ships everywhere on iOS that the grid renders at .full size (1- and 2-col density), the badge surfaces on Find / Decks pool / Collection / Rainbow rows without any per-call-site wiring (unlike Android which is opt-in per cell). Web parity queued.
- **tick 466 (Android — format-legality hint rollout to Featured/Decks/Collection/Rainbow grids)** — Tick 464 wired FindScreen.SearchResultsGrid only. This tick extends the rollout to four more surfaces: (a) FindScreen FeaturedCarousel, (b) DecksScreen card-pool grid, (c) CollectionScreen grid mode, (d) RainbowDetailScreen owned/missing grid. All four wire `formatLegalityHint = CardFormatEligibility.restrictedLegalAbbrev(card)`. Other Versions LazyRow on CollectionCardDetailScreen uses 80dp tiles (below the 100dp hint gate); badge won't render there but kept consistent for future polish. Wall mode + small-thumb sites (AddToCollection, list mode, DeckEditor in-deck row, suggestion thumbs) intentionally unwired — gate filters most anyway, and badge would compete with existing per-row chrome (cyan "in deck" checkmark + designation badge etc.).
- **tick 465 (opt + research — extract iOS PrintRunBadge, net −4 lines)** — Research: CI green across the last 12 commits. Tick 462's WAKE_UP entry flagged the iOS Other-Versions inline print-run badge as ~10-line duplication of BOBACardCell.printRunBadge. Extracted to a top-level `struct PrintRunBadge: View` in Design.swift; both call sites now `PrintRunBadge(label: label).padding(...)`. Single source of truth for orange-on-SSP / cyan-otherwise + semi-translucent backplate + corner radius. Standardized to font 10pt (was 9pt at Other Versions); negligible visual change at 80×112 tile size. KDoc trimmed to 2 lines after the extraction-stamp + tick-references rotted away. Net −4 lines (the struct-boilerplate offsets most of the inline-savings; the line-count opt is real but modest — the code-quality opt is bigger).
- **tick 464 (Android — per-cell format-legality hint badge, Discord backlog #4 carry-forward)** — Tick 460 flagged this as the remaining carry-forward after print-run badges closed (Card Detail chip strip shipped tick 179/203/etc; per-cell variant was open). New `CardFormatEligibility.restrictedLegalAbbrev(card): String?` in :core:domain returns the abbreviation of legal formats (e.g. "S+ C") ONLY when card is legal in less than all 4 formats — so the badge surfaces unusual cards instead of cluttering every cell. CONSTRAINED treated as legal for badge purposes (card is still playable; tier detail belongs on Card Detail). Added `formatLegalityHint: String? = null` opt-in param to BOBACardCell with bottom-leading `FormatLegalityHintBadge` (amber/BobaBrand.Orange — distinct from top-trailing cyan/orange print-run badge). Size gate: `maxWidth >= 100.dp` (wider than print-run's 80dp because multi-char abbrev needs more room). Wired FindScreen.SearchResultsGrid; rollout to other surfaces queued for follow-up Android ticks. iOS + web parity also queued.
- **tick 463 (web — print-run badge on CardDetail Other Versions, closes 3-platform trio)** — Closes the parity Android tick 461 + iOS tick 462 opened. `buildVersionsSection` in js/app.js now renders a `.version-print-run` overlay on each version tile when `printRunLabelFor(v)` returns non-null. Positioned top-right (no treatment ribbon to dodge on these mini-tiles, unlike main `.card-item` grid which used top-left at tick 458). Cyan default + orange `.--ssp` variant matching the mobile palette. Required wrapping the existing `.version-thumb` img/placeholder in a new `.version-thumb-wrap` span (position: relative) so the badge can absolute-position over the 72×96 thumb. Updated aria-label to include the print-run text (so screen readers announce "Maverick — Inspired Ink — /5" rather than just "Maverick — Inspired Ink"). Closes the 3-platform Other-Versions print-run-badge trio + cleans up the Discord backlog #7 carry-forward fully.
- **tick 462 (iOS v2.350 / 612 — print-run badge on CardDetail Other Versions, Android tick 461 parity)** — iOS HAS an "Other Versions" section (CardDetailView.swift L919-941) that the earlier audit missed; closes the 2-platform parity Android tick 461 opened. Each variant tile is `CardImageView(card: variant, size: .thumb)` at 80×112 — BOBACardCell's size==.full gate would filter the badge. Added an inline `.overlay(alignment: .topTrailing)` with the same orange-for-SSP / cyan-otherwise color rules + same semi-translucent black bg (alpha 0.62) as BOBACardCell.printRunBadge. Inline rather than via BOBACardCell to avoid plumbing a `forceShowBadge` override through the shared component. Pattern duplication (~10 lines) flagged for a future opt-tick extraction.
- **tick 461 (Android — print-run badge on CardDetail 'Other versions' + lower size gate)** — Tick 460 noted Discord backlog #7 had a remaining surface; this tick wires it. CollectionCardDetailScreen's "Other versions" LazyRow uses 80dp cells (right at the previous 120dp threshold). Lowered BOBACardCell's badge gate from `maxWidth >= 120.dp` to `maxWidth >= 80.dp` so the 80dp Other Versions tiles + 3-col density grids (~110dp) now show the badge. Still filters: 40dp suggestion thumbs + 60dp AddToCollection previews + 72dp DeckEditor in-deck rows (those would visually compete with the cyan "in deck" checkmark). Wired `printRunLabel = other.printRunLabel` on the Other Versions call site — semantically most useful here, where users tap a card to see all its prints (Inspired Ink /5 next to SSP next to Base Set is the typical lineup, and seeing print-run badges directly answers "which version is this?").
- **tick 460 (opt + research — Discord-mined backlog struck-through, net −8 lines)** — Milestone tick. Research: CI green across the last 10 commits. WAKE_UP.md at 83 lines / 44 ticks — within the rolling-window target post tick 450's trim; no WAKE_UP trim needed. Opt: the AUTONOMOUS_PROGRESS.md "Discord-mined feature backlog (May 2026 mining pass)" section listed 8 items per-doc-instruction "Strike out shipped items inline. Re-mine when ≤2 items left." but no strike-throughs had landed despite 7/8 items being closed. Trimmed the verbose section to a status summary with `~~`-strike notation on all 7 done items + the bold STILL OPEN marker on #6 (Wanted-list public sharing). The per-cell format-eligibility badge carry-forward for #4 now flagged inline. Dropped the obsolete "Tick 178's own next step" planning paragraph (Pick #1 → done, since item was already-shipped). Net −8 lines + the doc accurately reflects closure state for the first time since the 2026-05-21 mining pass.
- **tick 459 (Android — print-run badge rollout to Decks/Collection/Rainbow grids)** — Tick 456 wired Find SearchResultsGrid + FeaturedCarousel via the new `printRunLabel` param. This tick extends the rollout to three more high-traffic surfaces: (a) DecksScreen card-pool grid, (b) CollectionScreen grid mode, (c) RainbowDetailScreen owned/missing grid (especially apt for Inspired Ink rainbow goals). All four wired call sites pass `card.printRunLabel`; opt-in default null keeps the remaining call sites (CollectionScreen list-row 40dp + wall-mode + AddToCollectionSheet small thumb + DeckEditorSheet in-deck row + DeckWallSheet) unchanged. Wall mode skipped pending visual decision (badge would appear in exported share image; could be welcome or distracting depending on user intent).
- **tick 458 (web — print-run badge on card cells, closes 3-platform trio for Discord backlog #7)** — Android tick 456 + iOS tick 457 parity. Added `<span class="print-run-cell-badge">` to `buildCardElement` in js/app.js, gated on `printRunLabelFor(card)`. CSS `.print-run-cell-badge` positioned top-LEFT (not top-right like mobile — the existing `.card-treatment-ribbon` already lives at top-right on non-base treatments, so left avoids overlap on every Inspired Ink + Superfoil card). Cyan default + orange variant (`.print-run-cell-badge--ssp`) matching mobile palette. Container query `@container card-cell (max-width: 130px)` hides the badge at 'S' density — same legibility gate as Android `maxWidth >= 120.dp` + iOS `size == .full`. Closes the 3-platform per-cell print-run-badge parity (per-cell half of Discord backlog #7; the card-detail half closed at tick 198). NB: this commit also absorbs ~18 queued cron-firings that accumulated during the multi-step tick 458 work — the rolling 1-min cron caught up against a long sequence of file reads + edits, but the cron continues normally from here.
- **tick 457 (iOS v2.349 / 611 — print-run badge on card cells, Android tick 456 parity)** — 2-platform parity port. Added a top-trailing PrintRunBadge `.overlay` to BOBACardCell in Design.swift. Renders only when `size == .full` (iOS analog of Android's `maxWidth >= 120.dp` gate) AND `card.printRunLabel != nil`. Same color rules as Android: `Design.Colors.bobaOrange` for SSP / `Design.Colors.bobaCyan` for /5/10/25/50/Serial. Semi-translucent black background (alpha 0.62). Because BOBACardGridItem already passes `.full` for 1- and 2-col density (where the cell renders at the larger ≤1200px WebP), the badge appears everywhere BOBACardGridItem ships at lower densities — Find, Decks pool, Collection, Rainbow rows. .thumb-size call sites (scan chips, suggestion thumbnails, sidebar recents) skip the badge automatically. Web parity queued.
- **tick 456 (Android — print-run badge on Find card cells, Discord backlog #7)** — Closes the per-cell half of Discord backlog #7 on Android (Card Detail half shipped tick 189/197/198 across all 3 platforms). Added optional `printRunLabel: String? = null` param to BOBACardCell with default null so existing 15 call sites stay unchanged. When non-null + `maxWidth >= 120.dp`, renders a top-trailing PrintRunBadge — semi-translucent black background (alpha 0.62) + `BobaBrand.Cyan` for /5/10/25/50/Serial (Inspired Ink) or `BobaBrand.Orange` for SSP (Superfoil), per the CardDetailScreen color pattern (tick 189). Size gate (≥120dp) hides badge at 3-col density where labelSmall would be illegible. Wired from FindScreen.SearchResultsGrid (main catalog) + FeaturedCarousel cells; suggestion-list 40dp thumbs skipped (size gate filters them). Decks browser + Collection grid + Card Detail "Other versions" remain unwired — opt-in default null means safe to roll out incrementally. iOS + web parity queued.
- **tick 455 (opt + research, net −11 lines)** — Research: CI green across the last 7 commits. Backlog reaffirms the orphan DeckBuilderView (~1,200 lines) as biggest available but multi-step. Opt: DRY'd the Lucide eye / eye-slash SVG markup in js/auth.js that tick 453's password-toggle feature added. The eye SVG appeared 4× (modal markup × 2 + toggle handler × 2); the eye-slash SVG once. Hoisted to two top-level `EYE_SVG` + `EYE_SLASH_SVG` const strings inside the Auth IIFE; modal template + click handler now interpolate the consts. Net −11 lines. Other audit findings: (a) Android has the same risk after tick 454 added confirm-password — let me check that the BOBAIconTooltip + Icons.Default.Visibility pair isn't duplicated identically (it's not — the two trailingIcon blocks differ in which `confirmPasswordVisible` vs `passwordVisible` state they bind, so a wrapper composable would need to take a state binding, complexity offsets net savings). (b) iOS SignInView ditto — two HStack blocks with eye/eye-slash Buttons; the Swift compiler's verbose Button + Image + accessibilityLabel chain doesn't DRY as cleanly as JS template strings.
- **tick 454 (Android — Confirm password on sign-up, closes 3-platform trio)** — Audit during tick 452/453 password-toggle work: iOS + web both have a "Confirm password" field in sign-up mode; Android's `SignedOutContent` only renders a single password field regardless of `AuthMode`. Added `confirmPasswordText` + `confirmPasswordVisible` rememberSaveable state + a sign-up-only `OutlinedTextField` (with its own BOBAIconTooltip eye toggle matching the existing pattern). Submit handler now validates `passwordText != confirmPasswordText` → "Passwords don't match." feedback before calling `signUpWithEmail`. Also changed the password field's IME action from `Done` → `Next` on sign-up so the keyboard advances to the confirm field. Closes the 3-platform sign-up shape parity.
- **tick 453 (web — password-visibility eye toggle, closes 3-platform trio)** — Closes the 3-platform parity from Android tick 446 + iOS tick 452. Web auth modal (`js/auth.js`) had plain `<input type="password">` with no visibility toggle. Wrapped each input in `.form-password-wrap` with a `.form-password-toggle` button using the Lucide eye SVG. Click handler flips `input.type` between "password" / "text", swaps SVG between eye and eye-slash (Lucide shapes matching iOS SF Symbol idiom), and updates the aria-label + title between "Show password" / "Hide password". CSS: wrap is position:relative + the input gets `padding-right: 40px` so text doesn't slide under the icon. Tick 452's footer noted "lower priority because browser autofill mitigates the use case" — but mobile-web users especially benefit (autofill less reliable on touch); closes the trio properly.
- **tick 452 (iOS v2.348 / 610 — SignIn password-visibility toggle)** — Direct 2-platform parity with Android tick 446. iOS `SecureInputField` (UIViewRepresentable wrapping `UITextField`) had `isSecureTextEntry = true` hardcoded — no visibility toggle. Now accepts `isSecure: Bool = true` parameter (defaulting true for backward-compat) and re-asserts `field.isSecureTextEntry` in `updateUIView` so flips don't lose focus/caret. SignInView wires `@State passwordVisible` + `@State confirmPasswordVisible` to an eye/eye-slash Button next to each field (matching Android `Icons.Default.VisibilityOff` / `Visibility`). Carries both `.accessibilityLabel` + `.help` with dynamic text. Web has no visibility toggle on `<input type="password">` either — queued as web tick item, lower priority (web has browser-level autofill UX that mitigates the typo-confirmation use case).
- **tick 451 (Android — FilterSheet close-X tooltip)** — Audit at tick 446 noted close-X buttons on modal sheets are universal idiom — but the FilterSheet's close-X carries `contentDescription = "Done"` (semantic = "done filtering, apply"), which is unusual enough vs visual X that a tooltip genuinely disambiguates. Wrapped in `BOBAIconTooltip("Done filtering — close sheet")`. The remaining Android close-X buttons (AddToCollectionSheet, AddToDeckSheet, DeckWallSheet) all carry generic "Close" semantics — leaving those unwrapped per the universal-idiom audit. Otherwise audited: ScanScreen toolbar "Recent · ${size}" counter caps at 25 (locale-format wouldn't change display); deck "$total cards" counters cap well below 1k where locale-format matters; non-wavy progress indicators all migrated tick 176; BOBAHintBanner has zero call sites but exists as documented primitive per ANDROID-DESIGN.md §6.8.
- **tick 450 (opt + research, net −36 lines — milestone)** — Milestone tick. Research: CI green across the last 7 commits (tick 443-449). Audited remaining opt material: (a) 4 iOS relative-date helpers (StoreLocator / YouTubeVideo / PricingSection / Collection) differ enough in input type + output cases that parameterizing wouldn't be net-cleaner — same conclusion tick 449 reached on Android. (b) 7 occurrences of `ownedKeys.has(bobaId) || ownedKeys.has(cardNumber)` in js/collection.js could become a one-line helper but the wrapper would offset most of the per-site savings; net-neutral at best. (c) Orphan DeckBuilderView (~1,200 lines) still the biggest single opt available but needs Xcode-Cloud-verified extraction. Opt: trimmed 36 archived ticks (380-415) from WAKE_UP.md — same rolling-window approach as tick 435 (which trimmed 329-379). Polish patterns from the trimmed window are in git history + memory files; recent (416-449) ticks kept visible for context. Net −36 lines on WAKE_UP.md.
- **tick 449 (Android — BOBAIconTooltip doc trim, net −5 lines)** — Audit during tick: (a) 24 BOBAIconTooltip call sites now (rollout has grown from the 11 the doc-comment claimed at tick 400). (b) Two Android relative-date helpers — `relativeOrIsoDate` (Learn) and `relativeStoreDate` (Purchase) — look like web tick 448's DRY opportunity, but the behaviors diverge intentionally: Learn returns raw ISO for >5wk so collectors can grep feed history; Purchase returns "Nmo ago". No clean consolidation. Polish: trimmed BOBAIconTooltip's KDoc — the "Extracted at tick 400 after 11 near-identical TooltipBox call sites... (ticks 379 / 384 / 386 / 389 / 394 / 396 / 399)" preamble has rotted now that we're at 24 sites; per CLAUDE.md "don't reference the current task / fix / callers (rot as the codebase evolves)". Net −5 lines on the doc-comment; helper signature unchanged.
- **tick 448 (web — DRY out duplicate relativeDate helper, net −12 lines)** — Audit during tick: most web icon-only buttons already carry `title=` (verified via the 388/443 sweep). Substantive web work available: `_relativeDate` in store-locator.js (12 lines, tick 428) is byte-identical to `relativeDate` in app.js (12 lines). Tick 428's comment acknowledged the duplication ("Lives here because store-locator.js is its own IIFE and the app.js helper isn't exported"). Fixed by exposing `window.bobaRelativeDate = relativeDate` at end of app.js + replacing the store-locator helper with `window.bobaRelativeDate?.(rawUpdated)`. Safe because `renderSummary()` only fires after user interaction = after both IIFEs have initialized. Net −12 lines from store-locator.js; +3 lines in app.js (one `window.` export + 2-line comment). Real cross-file consolidation, not just provenance trim.
- **tick 447 (iOS v2.347 / 609 — CollectionCardDetail Edit + RainbowDetail Edit .help() tooltips)** — Continuing the iOS .help() rollout from tick 437/442. Two icon-only Buttons on Collection-side surfaces gained tooltip affordance: (a) CollectionCardDetailView's per-copy pencil/Edit button — direct 2-platform parity with Android tick 444's "Edit this copy" wrap; previously had NO accessibilityLabel either, added both `.accessibilityLabel("Edit this copy")` + `.help("Edit this copy")`. (b) RainbowDetailView's Edit-rainbow slider button (top-right when isCustom) had accessibilityLabel but no `.help()`; added matching `.help("Edit rainbow")`. Closes the 2-platform per-copy-Edit parity. (SourceKit env-scope warnings on both files are known LSP false positives.)
- **tick 446 (Android — Profile password-visibility toggle tooltip)** — Continuing the BOBAIconTooltip rollout. Audit found most remaining IconButtons on Android are universal idioms (back arrow, close X on sheets, search-clear X) where tooltips are debatable noise. One concrete polish remained: the email-sign-in password field's visibility toggle (eye / eye-off) at line 223 of ProfileSheet. Wrapped in `BOBAIconTooltip(...)` with the same dynamic text the existing contentDescription uses ("Hide password" / "Show password"). Particularly useful here — password fields are exactly where users get nervous about clicking-to-fire, and hover-confirm before clicking is the affordance value. iOS uses SwiftUI's native `SecureField` which has built-in toggle semantics; web's password field is a plain `<input type="password">` with no visibility toggle today.
- **tick 445 (opt + research, −9 lines)** — Research: CI green across the last 5 commits. Audit of opt material: (a) 5 `@available(iOS 18.0, *)` guards left in BOBAPlaybook/ — `feedback_ios_26_baseline` rule says "remove when touching the file but don't sweep proactively," so opt-tick-as-sweep is the proactive case. Skip. (b) 4 Android FQN `Icons.Default` call sites — net-zero (add import, remove FQN). (c) 16 `/* Tick N — ` CSS provenance comments — mostly carry real "why" content past the tick stamp; trim not worth bracket dance. (d) Orphan DeckBuilderView (~1,200 lines) still requires multi-step shared-helper relocation that's risky per `feedback_xcode_synchronized_groups` — deferred. Opt: trimmed SCRATCHPAD.md's "Pickup state — 2026-05-21 (pre-compaction)" section + collapsed "Recently shipped (archive)" prose to a clean pointer list. The pickup-state section was 245+ ticks old and the build-state info was rot; the recently-shipped prose duplicated git history. Memory-link pointers (the load-bearing part) preserved verbatim. Net −9 lines.
- **tick 444 (Android — CollectionCardDetail per-copy Edit + Delete tooltips)** — Continuing the BOBAIconTooltip rollout from ticks 426/429/436/439/441. Each row in the "Your copies (N)" list on CollectionCardDetailScreen has Edit + Delete IconButtons with `contentDescription` but no tooltip. The Delete button is colored `colorScheme.error` (red) which is particularly worth a confirmation hint — wrapped both in `BOBAIconTooltip("Edit this copy")` / `BOBAIconTooltip("Delete this copy")`. Same pattern as the tick-429 Custom Rainbows Edit+Delete tooltips.
- **tick 443 (web — Rainbow row gets a Share button)** — Closes the 3-platform Rainbow-share parity trio. iOS RainbowDetailView uses native ShareLink; Android tick 439 wrapped its Share IconButton in BOBAIconTooltip. Web had no Share affordance on rainbow rows at all. Added a `.rainbow-share-btn` next to the existing edit pencil (renders on every row — custom AND per-hero auto-rainbows). Click handler routes through `window.bobaShareTarget` w/ exact same text shape as Android tick 439: `"My {name} rainbow: 5 of 12 treatments (41%) · bobaplaybook.com"`. CSS reuses the edit-btn rule (combined selector — net-zero new style lines). Also added `title="Generate share image"` to the Find multiselect-wall button which had aria-label but no desktop hover hint.
- **tick 442 (iOS v2.346 / 608 — Hero Shot + Show wall share .help() tooltips)** — Continuing the iOS .help() rollout from tick 437 (ProfileView Copy + Share). Two icon-only Buttons in Collection-side surfaces had `.accessibilityLabel(...)` (or nothing) but no `.help(...)`: (a) CollectionCardDetailView's Hero Shot button now has `.help("Hero Shot — share a 3D video of this card")`; (b) ShowDetailView's Share-wall-image button had NO label at all — added both `.accessibilityLabel("Share wall image")` + `.help("Share wall image")` so VoiceOver users + iPad/Mac Catalyst hover users both gain the affordance hint. The Hero Shot button still carries an iOS-18 `@available` guard — `feedback_ios_26_baseline` rule says don't add new ones; this one is a leftover, queued for a future cleanup tick.
- **tick 441 (Android — DeckEditor 'Generate wall' + 'Remove from deck' tooltips)** — Continuing the BOBAIconTooltip rollout from ticks 426 / 429 / 436 / 439. Two IconButtons on DeckEditorSheet had `contentDescription` but no tooltip: (a) the toolbar Generate-deck-wall button (ViewModule icon — not self-evident from the icon alone) and (b) the per-card-row Delete button. Wrapped both in `BOBAIconTooltip(...)`. iOS DeckBuilderEditor already uses native ShareLink + SwiftUI built-in Delete swipe semantics; web has its own `db-wall-btn` with `title=` attribute. Single-platform polish.
- **tick 440 (opt + research, −9 lines)** — Research: CI green across the last 5 commits (tick 435-439). Backlog scan reaffirms orphan DeckBuilderView (~1,200 lines) is biggest available opt but needs dedicated Xcode-Cloud verified session. Android FQN sweeps remaining (LearnScreen / WatchPage / ScanScreen / etc.) are net-zero (add import, remove inline FQN). Opt: trimmed two stale meta-notes in AUTONOMOUS_PROGRESS.md — (a) "Audit agents in flight" section (8 lines) — Agents A/B/C ran 240+ ticks ago, findings already folded into the backlog; the section was just a status report that no longer tracks anything; (b) "Loop resumed post-compaction — tick 202+" subsection (4-line paragraph) — collapsed to a 3-line HTML comment; the original content was a status update saying tick 360 already trimmed something else. Net −9 lines.
- **tick 439 (Android — RainbowDetailScreen Share tooltip)** — Continuing the BOBAIconTooltip rollout from ticks 426 / 429 / 436. The Share IconButton in RainbowDetailScreen's TopAppBar actions slot (top-right of any rainbow detail page — auto OR custom) had `contentDescription` but no tooltip. Wrapped in `BOBAIconTooltip("Share rainbow progress")` so Chromebook + tablet users see the affordance hint before firing the Intent.ACTION_SEND chooser. iOS RainbowDetail already uses native ShareLink which has built-in label behavior; web equivalent (rainbow share) doesn't exist yet — single-platform polish, no iOS/web port owed.
- **tick 438 (web — Profile public-URL Share button + Copy tooltip)** — Closes both gaps tick 437's audit flagged. (a) Added `title="Copy public collection link"` to the existing copy button so desktop pointer-hover reveals the affordance (iOS .help() + Android BOBAIconTooltip parity from ticks 436/437). (b) Added a sibling Share button using the same Lucide share-2 SVG as the public-collection-share button; click routes through `window.bobaShareTarget` (Web Share API w/ clipboard fallback per WEB-DESIGN.md §8.2). Closes the 3-platform Profile public-URL Copy+Share parity trio — iOS + Android were carrying the Share affordance; web now matches.
- **tick 437 (iOS v2.345 / 607 — Profile Copy + Share .help() tooltips)** — Direct 2-platform parity with Android tick 436. ProfileView's public-collection-URL row had Copy + Share buttons with `.accessibilityLabel(...)` but no `.help(...)` — iPad trackpad / Mac Catalyst hover users got no affordance hint before tapping. Added `.help("Copy public collection link")` + `.help("Share public collection link")`. Web audit while I was here: `profile-public-url-copy` button has `aria-label="Copy URL"` but no `title=` attr, and web has no Share counterpart at all on the Profile public-URL row — both gaps queued for next web tick.
- **tick 436 (Android — Profile Copy + Share link tooltips)** — Continues the BOBAIconTooltip wrap-an-icon-only-button pattern from tick 426 (ScanReview Remove) and tick 429 (Custom Rainbows Edit/Delete). ProfileSheet's public-collection-URL row had Copy + Share IconButtons with `contentDescription` but no long-press / hover tooltip. Wrapped both in `BOBAIconTooltip("Copy public collection link")` / `BOBAIconTooltip("Share public collection link")` so Chromebook + tablet users see the affordance hint before firing the action. Same pattern as the rest of the tooltip rollout — content unchanged, M3 PlainTooltip via the tick-400 helper.
- **tick 435 (opt + research, −51 lines)** — Research: CI green across all recent commits (Android · build + pages both clean since tick 354). Stage B nightly Mirror sync from tick 361 was the last red gate; verified clean. Blog-digest scan: most-recent posts (2026-05-20 Open Letter to Live Sellers · 2026-05-19 Ripped Restock · 2026-05-15 St. Jude) already surface via `blog-feed.json` automatic mining — no new content extraction warranted. Backlog reaffirms orphan DeckBuilderView (~1,200 lines) remains the biggest available opt but needs Xcode-Cloud verified extraction. Opt: trimmed 51 lines of WAKE_UP.md ticks 329-379 — they're all in git history, the cross-platform polish patterns they document are already memorialized in memory files (feedback_kotlin_extension_calls_unqualified, feedback_swift_switch_expression_typecheck_timeout, feedback_compose_transformable_centroid_order etc.). Keeps the rolling window at ~55 most-recent ticks. Largest single-tick net-removal since tick 400's BOBAIconTooltip extraction (-59 lines).
- **tick 434 (Android — Card Detail copy counts locale-formatted)** — Web tick 433 parity. Two BOBASectionHeader titles in Android card-detail surfaces had bare-integer counts: CardDetailScreen "In your collection (N)" and CollectionCardDetailScreen "Your copies (N)". Both now use `NumberFormat.getInstance(Locale.US).format(...)` — "(1,234)" instead of "(1234)" for grail farmers with multi-copy stacks. Closes the 3-platform locale-format trio for card-detail copy counts (iOS already shows count via the existing tick-412 pattern on value-summary; per-card detail headers already used .formatted() where applicable).
- **tick 433 (web — Card Detail 'IN YOUR COLLECTION (N)' locale-formatted)** — `${entries.length}` rendered bare; collectors with 1,000+ copies of a card (think Bo Jackson grail farmers) saw "(1234)". Wrapped in `.toLocaleString()` — "(1,234)" reads cleaner. Minor but covers another counter that's no longer bare-integer across the app. Audit of other web counters found the rest already use `.toLocaleString()` (Find results, set/release option labels, Surprise pill, etc.).
- **tick 432 (iOS v2.344 / 606 — Find a Store 'Updated' relative-formatted)** — Closes the 3-platform freshness-stamp parity. iOS rendered "Updated 2026-05-17" raw ISO via `label.prefix(10)`; now renders "Updated 5d ago" via new `relativeStoreDate(_:)` fileprivate helper mirroring web tick 428 + Android tick 431. Same "today / Nd ago / Nw ago / Nmo ago" tiers, falls back to ISO prefix beyond 5wk.
- **tick 431 (Android — Find a Store 'Updated 5d ago' stamp)** — Tick 430's queued item delivered: wired `stores-manifest.json` `scraped_at` through StoreLocatorService → PurchaseViewModel → PurchaseScreen. Renders inline with the store count: "2,130 stores  ·  Updated 5d ago" via new `relativeStoreDate` helper mirroring js/store-locator.js's `_relativeDate` (tick 428). Closes 3-platform parity: iOS shows raw ISO `Updated 2026-05-22` (still queued for relative-format polish), web tick 428 + Android today both show "today / Nd ago / Nw ago".
- **tick 430 (opt + research, audit-only)** — Research findings: (a) Android Find a Store has no `scraped_at` "Updated" stamp at all — iOS shows raw ISO, web now shows relative (tick 428). Wiring `scraped_at` into Android store-manifest fetch is a multi-tick item, queued. (b) iOS Find uses `ProgressView()` during initial catalog load; Android already uses `BOBACardSkeleton()` placeholders (~3-5 row skeletons per DESIGN.md §6.7 universal-loading-states). iOS skeleton parity is a feature change, queued for a dedicated session. (c) AUTONOMOUS_PROGRESS backlog reaffirms: orphan DeckBuilderView delete (~1,200 lines) remains the biggest available opt — needs Xcode-Cloud-verified extraction not safely done in ambient cadence. No net-line opt this tick.
- **tick 429 (Android — Custom Rainbows per-row Edit+Delete tooltips)** — Extends tick-411's Manage Decks per-row IconButton tooltips pattern to the Custom Rainbows list. Long-press / hover surfaces "Edit custom rainbow" / "Delete custom rainbow" before the action fires — particularly useful for the red Delete button.
- **tick 428 (web — Find a Store 'Updated' stamp relative-formatted)** — `renderSummary` rendered "Updated 2026-05-22" as raw ISO. Now `_relativeDate(iso)` (local helper mirroring js/app.js's relativeDate since store-locator.js is its own IIFE) returns "today" / "5d ago" / "2w ago" instead, falling back to ISO for >5wk. Matches the freshness-stamp pattern shipped across events (web tick 218) + blog feeds (web tick 248) + Android (tick 419) + iOS (tick 252/416).
- **tick 427 (iOS v2.343 / 605 — Find a Store summary counts locale-formatted)** — Android tick 424 parity. `summaryLine` rendered 3 counts (`shown`, `total`, `hidden`) as bare integers — full catalog hits ~2,000 stores between indie + big-box. Wrapped each in `.formatted()` so "2,130 of 2,130 authorized retailers" reads cleaner than "2130 of 2130". Same pattern as Collection counts (tick 412) + Whatnot viewer counts (tick 407).
- **tick 426 (Android — ScanReviewSheet 'Remove from queue' tooltip)** — The X icon in a list-row trailing slot can be mistaken for "close the sheet"; BOBAIconTooltip("Remove from queue") on long-press / hover clarifies the action is per-row removal. Uses tick-400's helper. Compile clean.
- **tick 425 (opt + research, −31 lines)** — Research: backlog scan reaffirms the big remaining opts (orphan DeckBuilderView delete, M5.5 Practice port, Wanted-list sharing schema) are all multi-tick or external-blocked. Recent opt ticks (415/420) found audit-only material; tick 425 finds genuine net-removable in SCRATCHPAD. Opt: collapsed the 32-line "Pickup state — 2026-05-21 (pre-compaction)" subsection into a one-paragraph summary — the bulleted shipped-work record is all in git history + WAKE_UP "Things you need to do" + the documented memory files. Net −31 lines.
- **tick 424 (Android — Find a Store count locale-formatted)** — `"${state.filteredStores.size} stores"` in PurchaseScreen → bare integer. The full catalog hits ~2,000 stores (indie + big-box); "2,130 stores" reads cleaner than "2130 stores". Wrapped in `NumberFormat.getInstance(Locale.US).format(...)` — same pattern as tick 406 viewer count + tick 414 Collection count. Compile clean.
- **tick 423 (web — Recent BoBA news empty-state shows lastUpdated stamp)** — Symmetric with tick 418's events fix. `hydrateRecentBlogPosts` previously rendered just "No recent posts." when posts was empty, dropping the lastUpdated stamp. Now renders stamp + "No recent posts. Check back as the BoBA team publishes." hint. Closes the 3-platform empty-state-with-stamp trio for blog feeds (iOS tick 422 + Android tick 421 + web 423). Also harmonizes copy across all 3 platforms ("Check back as the BoBA team publishes").
- **tick 422 (iOS v2.342 / 604 — Recent BoBA news empty-state hint)** — Mirror of Android tick 421 + iOS tick 417 events. `RecentBoBANewsSection.body` was `if posts.isEmpty { EmptyView() } else { ... }` — the whole section silently disappeared on empty days. Restructured to always render the section header + lastUpdated stamp + either the posts list OR the "No recent posts. Check back as the BoBA team publishes." hint. Closes the 3-platform empty-state coverage for both events AND blog feeds across iOS / Android / web.
- **tick 421 (Android — Recent BoBA news empty-state hint)** — Companion to tick 417 (iOS events empty-state). Android Tournament Recent BoBA news section was gated on `blogPosts.isNotEmpty()` — the whole section + header + lastUpdated stamp disappeared on empty days. Restructured to always render the header + stamp + either the post list OR a "No recent posts. Check back as the BoBA team publishes." hint. Now users see the feed IS being refreshed even when no recent posts surfaced.
- **tick 420 (opt + research — Standing-priorities note corrected, audit only)** — Research: 60+ ticks deep into polish work. Audited iOS+Android Profile sheets for dead @State patterns — all live (`discordWorking`, `roleRequestForRole`, `walkthroughsResetMsg`, `copyConfirmed` etc. all have setters + reads). StoreLocatorView clean. No new dead code found beyond the tick-415 documented orphan `DeckBuilderView` struct (~1,200 lines, deferred to dedicated session). Opt: corrected the stale "Discord exports (research/)" reference in WAKE_UP standing priorities — research/ dir doesn't exist in repo; the active source is `docs/blog-digest.md` (daily cron). Tick-420 milestone shape would normally be a bigger opt (like tick 400's BOBAIconTooltip extraction), but the orphan DeckBuilderView delete needs deliberate Xcode-Cloud verified extraction not safely done in ambient cadence.
- **tick 419 (Android — Events 'Last refreshed' stamp uses relative date)** — Companion to tick 416. Android's Tournament Events list freshness stamp at line 498-507 was rendering raw ISO (`Last refreshed 2026-05-22`); now uses `relativeOrIsoDate(stamp)` matching the blog stamp (tick 416) and iOS LearnView L2771 + web app.js L892. Closes the 3-platform freshness-stamp parity for BOTH events and blog feeds.
- **tick 418 (web — empty-state events shows lastUpdated stamp too)** — Refining tick 417's iOS empty-state fix into web parity. `hydrateTournamentEvents` previously returned early on `events.length === 0`, keeping the static "events-empty" placeholder but losing the lastUpdated stamp. Now renders stamp + empty hint when events array is empty so users see the feed IS being refreshed even when there are no upcoming events to show. iOS tick 417 (Tournament section header + stamp + empty hint) parity.
- **tick 417 (iOS v2.341 / 603 — Tournament Events empty-state parity)** — iOS `UpcomingEventsSection.body` returned `EmptyView()` when events list was empty — meaning Tournament-tab users saw NO signal that upcoming events are tracked here. Android (LearnArticleScreen L508) shows "No events scheduled yet. Check back as the BoBA team announces dates." Restructured iOS to always render the section header + lastUpdated stamp + either the events grid OR the empty-hint copy. Closes the empty-state parity gap.
- **tick 416 (Android — Recent BoBA news 'Last refreshed' stamp uses relative date)** — Android freshness stamp rendered raw ISO (`Last refreshed 2026-05-22`); iOS LearnView L2657 + web app.js L831 both use a relativeDate helper that returns "today" / "yesterday" / "Nd ago" / "Nw ago". The same helper (`relativeOrIsoDate`) already exists at line 637 for per-post BlogPostRow dates — just hadn't been threaded through the bundle.lastUpdated stamp. One-line fix. Closes the 3-platform freshness-stamp parity.

## Things you (Ben) need to do

<!-- Anything that needs human action lands here. -->

### Still queued from before the loop started
1. **Manual-upload `app-release.aab`** (32 MB at `android/app/build/outputs/bundle/release/app-release.aab`) → Play Console → Internal Testing → Create new release.
2. **Walk through Play Console required forms** using `android/distribution/play-console-listing.md`.
3. **Upload icon + feature graphic + 5 screenshots** from `tools/play-assets/out/`.
4. **Re-capture screenshots 2–5** by booting the Pixel 9 Pro AVD from Android Studio's Device Manager + running `bash tools/play-assets/capture-screens.sh`.
5. **Add internal-testing emails**, share opt-in URL.
6. **Promote to Open Testing** track when ready.

## How to stop the loop

```sh
# In Claude Code:
/cron list
/cron delete <id>
```

Or just exit Claude — session-only cron dies automatically.
