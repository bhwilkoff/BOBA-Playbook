<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== Permissions ========== -->
    <!-- Camera + scan-feature pipeline (ANDROID-DEV.md §6) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Notifications (Android 13+ runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Biometric auth -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Internet (everything cloud-backed) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Chromebooks + tablets need to NOT require a touchscreen -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".BOBAApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BOBAPlaybook"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- Google Maps SDK key (Find a Store map). Injected from
             android/local.properties or the MAPS_API_KEY env var via
             manifestPlaceholders in app/build.gradle.kts. Restricted
             in Google Cloud Console to com.bobaplaybook.app + the
             upload + Play App Signing SHA-1 fingerprints. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- ML Kit Text Recognition (Latin) ships via Google Play
             Services dynamic delivery (DECISIONS.md #043 amended
             2026-05-26). This meta-data triggers a one-time model
             download at install time so the first scan doesn't
             have to wait for it. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:theme="@style/Theme.BOBAPlaybook.Splash">

            <!-- Default launcher entry -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links — verified HTTPS deep links to bobaplaybook.com  -->
            <!-- (ANDROID-DEV.md §14.2; assetlinks.json at .well-known/) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="bobaplaybook.com" />
                <data android:pathPrefix="/u" />
                <data android:pathPrefix="/card" />
                <data android:pathPrefix="/deck" />
                <data android:pathPrefix="/learn" />
                <data android:pathPrefix="/search" />
            </intent-filter>

            <!-- Custom-scheme deep links (OAuth callbacks + intra-app) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bobaplaybook" />
            </intent-filter>
        </activity>

    </application>
</manifest>
