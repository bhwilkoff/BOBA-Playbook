<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Wall PNG exports + scan capture-history images live in the cache
         directory under wall/ and scan/. FileProvider exposes them at
         content://com.bobaplaybook.app.fileprovider/cached_images/... so
         Intent.ACTION_SEND receivers can read without storage permissions. -->
    <cache-path name="cached_images" path="." />
</paths>
