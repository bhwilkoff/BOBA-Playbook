Drop iPad screenshots here — LANDSCAPE orientation.

Required filenames (landscape):
  home.png
  search.png
  scan.png
  collect.png
  pricing.png
  play.png
  decks.png

Capture from a 12.9" iPad Pro (2732×2048) or 13" iPad Pro M4 (2752×2064),
LANDSCAPE. Both export sizes use the same source images — the layout designs at
2752×2064 and scales down for the 12.9" export automatically.

What to capture (rotate the iPad to landscape first):
  home.png      Find tab no-search state with featured ribbons (NavSplitView visible)
  search.png    Find search results — scope chips visible, grid populated
  scan.png      Scan overlay — guide rectangle visible, ideally with a card framed
  collect.png   Collection NavigationSplitView — lens picker (My Cards) + grid
  pricing.png   Card detail showing the Pricing section (Buy Now + Sold history)
  play.png      Learn tab — Rules / Strategy / Glossary article visible
  decks.png     Decks tab — 3-column NavigationSplitView (saved decks | pool | editor) populated

The slides fall back to a placeholder if a file is missing, so you can preview
the layout before all seven are captured.
